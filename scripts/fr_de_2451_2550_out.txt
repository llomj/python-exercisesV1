  2451: `Double underscore prefix (__) triggers name mangling, making attributes "private" by changing their name. Si class MyClass: def __init__(self): self.__x = 1; obj = MyClass(); obj.__x, alors obj.__x lève an AttributeError car Python automatically renames __x to _MyClass__x (adding the class name prefix). Ce/Ceci est name mangling - Python changes the attribute name to include the class name, making it harder to access from outside the class. Le/La attribute still exists, but with a different name (_MyClass__x), so direct access via __x fails.

Name mangling avec double underscore :
• obj.__x lève AttributeError
• Python renames __x to _MyClass__x
• Name mangling adds class name prefix
• Direct access via __x fails
• Lève AttributeError

Comment ça fonctionne :
• self.__x = 1 sets attribute
• Python renames __x to _MyClass__x internally
• obj.__x tries to access __x
• Python looks for __x (not found, it's _MyClass__x now)
• Lève AttributeError: 'MyClass' object a no attribute '__x'

Exemple :
class MyClass:
    def __init__(self):
        self.__x = 1  # Name mangled to _MyClass__x
obj = MyClass()
obj.__x                # AttributeError (name mangled)
obj._MyClass__x       # 1 (mangled name works)

Utilisations courantes :
• Private attributes: self.__attr (name mangled, harder to access)
• Encapsulation: prevent direct access to internal attributes
• Name mangling
• Object-oriented programming

Exemple : Si class MyClass: def __init__(self): self.__x = 1; obj = MyClass(); obj.__x, alors obj.__x lève an AttributeError car double underscore prefix triggers name mangling, renaming __x to _MyClass__x.
`,
  2452: `Name mangling renames attributes with double underscore prefix by adding the class name. Si class MyClass: def __init__(self): self.__x = 1; obj = MyClass(); obj._MyClass__x, alors obj._MyClass__x renvoie 1 car Python renames __x to _MyClass__x (class name + attribute name). While name mangling makes attributes harder to access, they're not truly private - you peut still access them using the mangled name. Ce/Ceci est Python's way of providing "privacy" - it's a convention, not enforced security.

Le name mangling crée le nom manglé :
• obj._MyClass__x renvoie 1
• __x est renamed to _MyClass__x
• Mangled name: _ClassName__attribute
• Can still access using mangled name
• Résultat : 1

Comment ça fonctionne :
• self.__x = 1 sets attribute
• Python renames __x to _MyClass__x
• obj._MyClass__x accesses mangled name
• Attribute exists as _MyClass__x
• Résultat : 1

Exemple :
class MyClass:
    def __init__(self):
        self.__x = 1  # Renamed to _MyClass__x
obj = MyClass()
obj._MyClass__x       # 1 (access via mangled name)
obj.__x               # AttributeError (original name ne ... pas work)

Utilisations courantes :
• Understanding mangling: __attr becomes _ClassName__attr
• Accessing mangled attributes: obj._ClassName__attr
• Name mangling
• Encapsulation

Exemple : Si class MyClass: def __init__(self): self.__x = 1; obj = MyClass(); obj._MyClass__x, alors obj._MyClass__x renvoie 1 car name mangling renames __x to _MyClass__x, and you peut still access it using the mangled name.
`,
  2453: `Class attributes with double underscore prefix don't get name mangled car name mangling only applies to instance attributes (those set with self). Si class MyClass: __x = 1; MyClass.__x, alors MyClass.__x renvoie 1 car __x est a class attribute (defined at class level, not with self), so Python ne ... pas apply name mangling to it. Name mangling only happens for instance attributes (self.__x), not class attributes (__x at class level).

Class attribute ne ... pas mangle:
• MyClass.__x renvoie 1
• __x est class attribute (not self.__x)
• Name mangling only applies to instance attributes
• Class attributes with __ don't mangle
• Résultat : 1

Comment ça fonctionne :
• class MyClass: __x = 1 defines class attribute
• __x est at class level (not self.__x)
• Name mangling only for instance attributes
• MyClass.__x accesses class attribute directly
• Résultat : 1

Exemple :
class MyClass:
    __x = 1  # Class attribute (ne ... pas mangle)
MyClass.__x            # 1 (access directly, no mangling)
# vs
class MyClass:
    def __init__(self):
        self.__x = 1  # Instance attribute (mangles to _MyClass__x)

Utilisations courantes :
• Class attributes: __attr at class level (ne ... pas mangle)
• Understanding mangling: only instance attributes get mangled
• Name mangling
• Class vs instance attributes

Exemple : Si class MyClass: __x = 1; MyClass.__x, alors MyClass.__x renvoie 1 car class attributes with __ prefix don't get name mangled - name mangling only applies to instance attributes (self.__x).
`,
  2454: `Single underscore prefix (_) est a naming convention indicating "protected" or "internal use" attributes, but Python ne ... pas enforce it. Si class MyClass: def __init__(self): self._x = 1; obj = MyClass(); obj._x, alors obj._x renvoie 1 car single underscore est just a convention - it ne ... pas trigger name mangling or prevent access. It's a signal to other developers that thest attribute est intended for internal use, but Python permet access anyway. Ce/Ceci est different from double underscore (__), which triggers name mangling.

Single underscore convention:
• obj._x renvoie 1
• Single underscore est convention only
• Not enforced by Python
• Signals "internal use" but accessible
• Résultat : 1

Comment ça fonctionne :
• self._x = 1 sets attribute with single underscore
• Python ne ... pas mangle or restrict access
• obj._x accesses attribute directly
• Convention indicates internal use
• Résultat : 1

Exemple :
class MyClass:
    def __init__(self):
        self._x = 1  # Protected (convention)
obj = MyClass()
obj._x                # 1 (accessible, but convention says "internal")

Utilisations courantes :
• Protected attributes: self._attr (convention, not enforced)
• Internal use: signal that attribute est for internal use
• Naming conventions
• Encapsulation

Exemple : Si class MyClass: def __init__(self): self._x = 1; obj = MyClass(); obj._x, alors obj._x renvoie 1 car single underscore est a convention indicating "protected" attributes, but Python ne ... pas enforce it - the attribute est still accessible.
`,
  2455: `Name mangling prevents child classes from accessing parent's private attributes. Si class Parent: def __init__(self): self.__x = 1; class Child(Parent): def method(self): return self.__x; Child().method(), alors Child().method() lève an AttributeError car Parent's __x est mangled to _Parent__x, but Child's method tries to access __x, which would be mangled to _Child__x (different name). Name mangling includes the class name, so each class a its own mangled namespace, preventing child classes from accidentally accessing parent's private attributes.

Name mangling prevents child access:
• Child().method() lève AttributeError
• Parent.__x est mangled to _Parent__x
• Child tries to access __x (would be _Child__x)
• Different mangled names
• Lève AttributeError

Comment ça fonctionne :
• Parent.__init__ sets self.__x = 1 (mangled to _Parent__x)
• Child().method() tries to access self.__x
• In Child context, __x would mangle to _Child__x
• _Child__x ne ... pas exist (attribute est _Parent__x)
• Lève AttributeError

Exemple :
class Parent:
    def __init__(self):
        self.__x = 1  # Mangled to _Parent__x
class Child(Parent):
    def method(self):
        return self.__x  # Would mangle to _Child__x (ne ... pas exist)
Child().method()        # AttributeError (can't access parent's __x)

Utilisations courantes :
• Private attributes: name mangling prevents child access
• Encapsulation: each class a its own mangled namespace
• Name mangling
• Inheritance

Exemple : Si class Parent: def __init__(self): self.__x = 1; class Child(Parent): def method(self): return self.__x; Child().method(), alors Child().method() lève an AttributeError car name mangling prevents child classes from accessing parent's private attributes - Parent.__x becomes _Parent__x, but Child tries to access __x which would be _Child__x.
`,
  2456: `You peut access a parent's mangled attribute by using the mangled name with the parent class name prefix. Si class Parent: def __init__(self): self.__x = 1; class Child(Parent): def method(self): return self._Parent__x; Child().method(), alors Child().method() renvoie 1 car self._Parent__x accesses the mangled name directly. Parent's __x est mangled to _Parent__x, and by using the full mangled name, you peut access it from the child class. Ce/Ceci shows that name mangling est not true privacy - it's just a naming convention that makes access harder.

Access mangled name directly:
• Child().method() renvoie 1
• self._Parent__x accesses mangled name
• Parent.__x est mangled to _Parent__x
• Using mangled name works
• Résultat : 1

Comment ça fonctionne :
• Parent.__init__ sets self.__x = 1 (mangled to _Parent__x)
• Child().method() accesses self._Parent__x
• Uses full mangled name with class prefix
• Attribute exists as _Parent__x
• Résultat : 1

Exemple :
class Parent:
    def __init__(self):
        self.__x = 1  # Mangled to _Parent__x
class Child(Parent):
    def method(self):
        return self._Parent__x  # Access via mangled name
Child().method()        # 1 (peut access parent's mangled attribute)

Utilisations courantes :
• Accessing mangled attributes: obj._ClassName__attr
• Understanding mangling: peut access if you know the mangled name
• Name mangling
• Inheritance

Exemple : Si class Parent: def __init__(self): self.__x = 1; class Child(Parent): def method(self): return self._Parent__x; Child().method(), alors Child().method() renvoie 1 car you peut access the mangled name with the class name prefix - Parent.__x becomes _Parent__x, and accessing it directly works.
`,
  2457: `Double underscore on both sides (__x__) ne ... pas trigger name mangling car Python reserves thest pattern for special methods (magic methods). Si class MyClass: def __init__(self): self.__x__ = 1; obj = MyClass(); obj.__x__, alors obj.__x__ renvoie 1 car attributes with double underscores on both sides (like __str__, __len__, __init__) sont reserved for Python's special methods and don't get name mangled. Ce/Ceci permet you to define custom special methods without interference from name mangling.

Double underscore both sides ne ... pas mangle:
• obj.__x__ renvoie 1
• __x__ a double underscore on both sides
• Reserved pattern for special methods
• Doesn't trigger name mangling
• Résultat : 1

Comment ça fonctionne :
• self.__x__ = 1 sets attribute
• Python sees __x__ (double underscore both sides)
• Recognizes as special method pattern
• Doesn't apply name mangling
• obj.__x__ accesses attribute directly
• Résultat : 1

Exemple :
class MyClass:
    def __init__(self):
        self.__x__ = 1  # Doesn't mangle (special method pattern)
obj = MyClass()
obj.__x__              # 1 (no mangling, accessible)
# vs
self.__x = 1            # Mangles to _MyClass__x

Utilisations courantes :
• Special methods: __str__, __len__, etc. (don't mangle)
• Understanding mangling: only __attr (not __attr__) mangles
• Name mangling
• Special methods

Exemple : Si class MyClass: def __init__(self): self.__x__ = 1; obj = MyClass(); obj.__x__, alors obj.__x__ renvoie 1 car double underscore on both sides ne ... pas trigger name mangling - thest pattern est reserved for special methods.
`,
  2458: `Getter methods provide controlled access to protected (single underscore) attributes. Si class MyClass: def __init__(self): self._x = 1; def get_x(self): return self._x; obj = MyClass(); obj.get_x(), alors obj.get_x() renvoie 1 car get_x() est a getter method that fournit controlled access to the protected attribute _x. While _x could be accessed directly (obj._x), using a getter method fournit a controlled interface, allowing the class to add validation, transformation, or logging in the future without changing the external API.

Getter method for protected attribute:
• obj.get_x() renvoie 1
• get_x() fournit controlled access
• Accesses protected attribute _x
• Returns value through method
• Résultat : 1

Comment ça fonctionne :
• obj.get_x() calls getter method
• get_x() executes: return self._x
• Accesses protected attribute _x = 1
• Returns value through method
• Résultat : 1

Exemple :
class MyClass:
    def __init__(self):
        self._x = 1  # Protected attribute
    def get_x(self):
        return self._x  # Getter method
obj = MyClass()
obj.get_x()            # 1 (controlled access)
obj._x                 # 1 (direct access also works, but getter preferred)

Utilisations courantes :
• Controlled access: getter methods provide interface to protected attributes
• Encapsulation: hide implementation details behind methods
• Getter methods
• Object-oriented programming

Exemple : Si class MyClass: def __init__(self): self._x = 1; def get_x(self): return self._x; obj = MyClass(); obj.get_x(), alors obj.get_x() renvoie 1 car getter methods provide controlled access to protected attributes, allowing the class to manage how attributes sont accessed.
`,
  2459: `Getter methods peut access mangled (double underscore) attributes from within the class. Si class MyClass: def __init__(self): self.__x = 1; def get_x(self): return self.__x; obj = MyClass(); obj.get_x(), alors obj.get_x() renvoie 1 car when you access __x from within the class (in a method), Python automatically uses the mangled name (_MyClass__x). Inside the class, you peut use __x and Python knows to look for _MyClass__x. Ce/Ceci permet getter methods to access private (mangled) attributes while keeping them hidden from external access.

Getter accesses mangled name:
• obj.get_x() renvoie 1
• get_x() accesses __x from within class
• Python automatically uses mangled name _MyClass__x
• Works from within class
• Résultat : 1

Comment ça fonctionne :
• obj.get_x() calls getter method
• get_x() executes: return self.__x
• Inside class, __x refers to _MyClass__x (mangled)
• Python automatically resolves to mangled name
• Résultat : 1

Exemple :
class MyClass:
    def __init__(self):
        self.__x = 1  # Mangled to _MyClass__x
    def get_x(self):
        return self.__x  # Inside class, __x works (auto-mangled)
obj = MyClass()
obj.get_x()            # 1 (works from within class)
obj.__x                # AttributeError (ne ... pas work from outside)

Utilisations courantes :
• Private attributes: getter methods peut access mangled attributes
• Encapsulation: hide attributes, provide controlled access
• Getter methods
• Name mangling

Exemple : Si class MyClass: def __init__(self): self.__x = 1; def get_x(self): return self.__x; obj = MyClass(); obj.get_x(), alors obj.get_x() renvoie 1 car getter methods peut access mangled names from within the class - Python automatically resolves __x to _MyClass__x inside the class.
`,
  2460: `Setter methods peut modify mangled (double underscore) attributes from within the class. Si class MyClass: def __init__(self): self.__x = 1; def set_x(self, val): self.__x = val; obj = MyClass(); obj.set_x(2); obj.get_x() if hasattr(obj, 'get_x') else obj._MyClass__x, alors it renvoie 2 car set_x() est a setter method that modifies the mangled attribute __x. Inside the class, __x automatically refers to _MyClass__x, so set_x() peut modify it. Si get_x() exists, it renvoie the updated value (2); otherwise, accessing _MyClass__x directly also renvoie 2.

Setter modifies mangled attribute:
• obj.set_x(2) modifies __x
• Inside class, __x refers to _MyClass__x
• set_x() modifies mangled attribute
• obj.get_x() or obj._MyClass__x renvoie 2
• Résultat : 2

Comment ça fonctionne :
• obj.set_x(2) calls setter method
• set_x() executes: self.__x = val
• Inside class, __x refers to _MyClass__x
• Sets _MyClass__x = 2
• obj.get_x() or obj._MyClass__x renvoie 2
• Résultat : 2

Exemple :
class MyClass:
    def __init__(self):
        self.__x = 1  # Mangled to _MyClass__x
    def set_x(self, val):
        self.__x = val  # Modifies mangled attribute
    def get_x(self):
        return self.__x
obj = MyClass()
obj.set_x(2)            # Modifies __x
obj.get_x()            # 2 (updated value)

Utilisations courantes :
• Private attributes: setter methods peut modify mangled attributes
• Encapsulation: controlled modification of private attributes
• Setter methods
• Name mangling

Exemple : Si class MyClass: def __init__(self): self.__x = 1; def set_x(self, val): self.__x = val; obj = MyClass(); obj.set_x(2); obj.get_x() if hasattr(obj, 'get_x') else obj._MyClass__x, alors it renvoie 2 car setter methods peut modify mangled attributes from within the class.
`,
  2461: `Le/La @property decorator fournit controlled read access to attributes. Si class MyClass: def __init__(self): self._x = 1; @property; def x(self): return self._x; obj = MyClass(); obj.x, alors obj.x renvoie 1 car @property converts the method x() into a property, allowing you to access it like an attribute (obj.x instead of obj.x()). Le/La property getter renvoie the value of the protected attribute _x, providing controlled access while keeping _x protected. Ce/Ceci est a cleaner interface than using getter methods like get_x().

Property fournit controlled access:
• obj.x renvoie 1
• @property makes method accessible as attribute
• Property getter renvoie self._x
• Clean interface: obj.x (not obj.get_x())
• Résultat : 1

Comment ça fonctionne :
• obj.x accesses property
• Python calls property getter: @property def x(self)
• Getter executes: return self._x
• Returns protected attribute value
• Résultat : 1

Exemple :
class MyClass:
    def __init__(self):
        self._x = 1  # Protected attribute
    @property
    def x(self):
        return self._x  # Property getter
obj = MyClass()
obj.x                        # 1 (property access, clean interface)

Utilisations courantes :
• Controlled access: @property def attr(self): return self._attr
• Clean interface: obj.attr instead of obj.get_attr()
• Properties
• Encapsulation

Exemple : Si class MyClass: def __init__(self): self._x = 1; @property; def x(self): return self._x; obj = MyClass(); obj.x, alors obj.x renvoie 1 car @property fournit controlled read access, making the method accessible as an attribute.
`,
  2462: `Property setters peut validate or transform values before storing them. Si class MyClass: def __init__(self): self._x = 1; @property; def x(self): return self._x; @x.setter; def x(self, val): self._x = val * 2; obj = MyClass(); obj.x = 5; obj.x, alors obj.x renvoie 10 car the setter multiplies the value by 2 before storing it (self._x = val * 2 = 5 * 2 = 10). Quand on assign obj.x = 5, the setter est called, transforms the value (5 * 2 = 10), and stores 10. Ce/Ceci permet properties to enforce business rules, validate input, or transform data before storage.

Property setter transforms value:
• obj.x = 5 calls setter
• Setter executes: self._x = val * 2 = 5 * 2 = 10
• Stores transformed value: 10
• obj.x calls getter: return self._x
• Résultat : 10

Comment ça fonctionne :
• obj.x = 5 calls setter: @x.setter def x(self, val)
• Setter transforms: self._x = val * 2
• Evaluates: 5 * 2 = 10
• Stores: self._x = 10
• obj.x returns: 10

Exemple :
class MyClass:
    def __init__(self):
        self._x = 1
    @property
    def x(self):
        return self._x
    @x.setter
    def x(self, val):
        self._x = val * 2  # Transforms value
obj = MyClass()
obj.x = 5                    # Stores 10 (5 * 2)
obj.x                        # 10 (transformed value)

Utilisations courantes :
• Value transformation: @x.setter def x(self, val): self._x = transform(val)
• Validation: @x.setter def x(self, val): if valid: self._x = val
• Properties
• Encapsulation

Exemple : Si class MyClass: def __init__(self): self._x = 1; @property; def x(self): return self._x; @x.setter; def x(self, val): self._x = val * 2; obj = MyClass(); obj.x = 5; obj.x, alors obj.x renvoie 10 car the property setter transforms the value before storing it (5 * 2 = 10).
`,
  2463: `Properties peut access mangled (double underscore) attributes. Si class MyClass: def __init__(self): self.__x = 1; @property; def x(self): return self.__x; obj = MyClass(); obj.x, alors obj.x renvoie 1 car the property getter peut access the mangled attribute __x from within the class. Inside the class, __x automatically refers to _MyClass__x (the mangled name), so the property peut retrieve it. Ce/Ceci permet properties to provide controlled access to private (mangled) attributes while keeping them hidden from direct external access.

Property accesses mangled attribute:
• obj.x renvoie 1
• Property getter accesses __x
• Inside class, __x refers to _MyClass__x
• Property peut access mangled name
• Résultat : 1

Comment ça fonctionne :
• obj.x accesses property
• Property getter executes: return self.__x
• Inside class, __x refers to _MyClass__x (mangled)
• Python automatically resolves to mangled name
• Résultat : 1

Exemple :
class MyClass:
    def __init__(self):
        self.__x = 1  # Mangled to _MyClass__x
    @property
    def x(self):
        return self.__x  # Property peut access mangled name
obj = MyClass()
obj.x                        # 1 (property accesses mangled attribute)
obj.__x                      # AttributeError (direct access fails)

Utilisations courantes :
• Private attributes: @property def attr(self): return self.__attr
• Controlled access: properties provide interface to mangled attributes
• Properties
• Name mangling

Exemple : Si class MyClass: def __init__(self): self.__x = 1; @property; def x(self): return self.__x; obj = MyClass(); obj.x, alors obj.x renvoie 1 car properties peut access mangled attributes - inside the class, __x automatically refers to the mangled name.
`,
  2464: `A property setter that ne ... pas store the value ne ... pas change the property. Si class MyClass: @property; def x(self): return 1; @x.setter; def x(self, val): pass; obj = MyClass(); obj.x = 5; obj.x, alors obj.x renvoie 1 car the setter a pass (does nothing), so it ne ... pas store the value. Le/La assignment obj.x = 5 calls the setter, but the setter ignores the value. Le/La getter still renvoie 1, so the property value remains unchanged. Ce/Ceci pattern peut be used to create read-only properties that accept assignments but ignore them.

Setter ne ... pas store:
• obj.x = 5 calls setter
• Setter executes: pass (does nothing)
• Value 5 est ignored (not stored)
• obj.x calls getter: return 1
• Résultat : 1 (unchanged)

Comment ça fonctionne :
• obj.x = 5 calls setter: @x.setter def x(self, val)
• Setter executes: pass (does nothing)
• Value 5 est not stored
• obj.x calls getter: @property def x(self)
• Getter returns: 1 (unchanged)
• Résultat : 1

Exemple :
class MyClass:
    @property
    def x(self):
        return 1
    @x.setter
    def x(self, val):
        pass  # Doesn't store value
obj = MyClass()
obj.x = 5                    # Calls setter (ignores value)
obj.x                        # 1 (getter unchanged)

Utilisations courantes :
• Read-only simulation: setter that ignores values
• Validation-only setters: setter that validates but ne ... pas store
• Properties
• Encapsulation

Exemple : Si class MyClass: @property; def x(self): return 1; @x.setter; def x(self, val): pass; obj = MyClass(); obj.x = 5; obj.x, alors obj.x renvoie 1 car a setter that ne ... pas store the value ne ... pas change the property, so the getter still renvoie 1.
`,
  2465: `Le/La property() function peut be created with getter and setter functions as arguments. Si class MyClass: def __init__(self): self._x = 1; def get_x(self): return self._x; def set_x(self, val): self._x = val; x = property(get_x, set_x); obj = MyClass(); obj.x = 5; obj.x, alors obj.x renvoie 5 car property(get_x, set_x) creates a property where get_x est the getter and set_x est the setter. Ce/Ceci est an alternative syntax to using @property and @x.setter decorators. Quand on assign obj.x = 5, it calls set_x(5), which sets self._x = 5. Quand on access obj.x, it calls get_x(), which renvoie self._x = 5.

property() with getter and setter:
• obj.x = 5 calls set_x(5)
• set_x sets self._x = 5
• obj.x calls get_x()
• get_x renvoie self._x = 5
• Résultat : 5

Comment ça fonctionne :
• property(get_x, set_x) creates property
• get_x est getter function
• set_x est setter function
• obj.x = 5 calls set_x(5)
• set_x sets self._x = 5
• obj.x calls get_x()
• Résultat : 5

Exemple :
class MyClass:
    def __init__(self):
        self._x = 1
    def get_x(self):
        return self._x
    def set_x(self, val):
        self._x = val
    x = property(get_x, set_x)  # Alternative to @property
obj = MyClass()
obj.x = 5                    # Calls set_x(5)
obj.x                        # 5 (calls get_x())

Utilisations courantes :
• Property creation: x = property(getter, setter) (alternative syntax)
• Functional style: property() function instead of decorators
• Properties
• Encapsulation

Exemple : Si class MyClass: def __init__(self): self._x = 1; def get_x(self): return self._x; def set_x(self, val): self._x = val; x = property(get_x, set_x); obj = MyClass(); obj.x = 5; obj.x, alors obj.x renvoie 5 car property() peut be created with getter and setter functions, and the setter stores the value.
`,
  2466: `Le/La @x.deleter decorator defines a property deleter that fournit controlled deletion. Si class MyClass: def __init__(self): self._x = 1; @property; def x(self): return self._x; @x.deleter; def x(self): del self._x; obj = MyClass(); del obj.x; hasattr(obj, '_x'), alors hasattr(obj, '_x') renvoie False car @x.deleter defines what happens when you delete the property, and del obj.x calls the deleter, which deletes self._x. Ce/Ceci fournit controlled deletion of attributes, allowing you to add cleanup logic or validation before deletion.

Property deleter:
• del obj.x calls deleter
• @x.deleter defines deletion behavior
• Deleter executes: del self._x
• Attribute _x est deleted
• hasattr(obj, '_x') renvoie False

Comment ça fonctionne :
• obj._x = 1 sets attribute
• del obj.x calls deleter: @x.deleter def x(self)
• Deleter executes: del self._x
• Attribute _x est deleted
• hasattr(obj, '_x') vérifie if attribute exists
• Résultat : False

Exemple :
class MyClass:
    def __init__(self):
        self._x = 1
    @property
    def x(self):
        return self._x
    @x.deleter
    def x(self):
        del self._x  # Controlled deletion
obj = MyClass()
del obj.x                    # Calls deleter
hasattr(obj, '_x')          # False (deleted)

Utilisations courantes :
• Controlled deletion: @x.deleter def x(self): cleanup logic
• Property deletion: del obj.property (uses deleter)
• Properties
• Encapsulation

Exemple : Si class MyClass: def __init__(self): self._x = 1; @property; def x(self): return self._x; @x.deleter; def x(self): del self._x; obj = MyClass(); del obj.x; hasattr(obj, '_x'), alors hasattr(obj, '_x') renvoie False car @x.deleter fournit controlled deletion, and del obj.x calls the deleter, which deletes self._x.
`,
  2467: `A property without a setter est read-only - you ne peut pas assign to it. Si class MyClass: def __init__(self): self._x = 1; @property; def x(self): return self._x; obj = MyClass(); obj.x = 2, alors obj.x = 2 lève an AttributeError car the property only a a getter (defined by @property), but no setter. To make a property writable, you need to define a setter using @x.setter. Without a setter, the property est read-only, and any attempt to assign to it lève an AttributeError.

Property without setter:
• obj.x = 2 lève AttributeError
• Property only a getter (no setter)
• Cannot assign to read-only property
• Lève AttributeError: can't set attribute
• Error: AttributeError

Comment ça fonctionne :
• obj.x = 2 attempts to assign to property
• Property a no setter defined
• Python ne peut pas set read-only property
• Lève AttributeError: can't set attribute

Exemple :
class MyClass:
    def __init__(self):
        self._x = 1
    @property
    def x(self):
        return self._x  # Only getter, no setter
obj = MyClass()
obj.x                        # 1 (read works)
obj.x = 2                    # AttributeError (no setter, read-only)

Utilisations courantes :
• Read-only properties: @property def value(self): return self._value
• Computed properties: @property def area(self): return self.width * self.height
• Properties
• Encapsulation

Exemple : Si class MyClass: def __init__(self): self._x = 1; @property; def x(self): return self._x; obj = MyClass(); obj.x = 2, alors obj.x = 2 lève an AttributeError car a property without a setter est read-only and ne peut pas be assigned to.
`,
  2468: `Property getters peut validate or transform values when they're accessed. Si class MyClass: def __init__(self): self._x = 1; @property; def x(self): return self._x if self._x > 0 else 0; obj = MyClass(); obj._x = -1; obj.x, alors obj.x renvoie 0 car the property getter vérifie if self._x > 0, and if not, it renvoie 0 instead of the actual value. Even though obj._x = -1 (set directly), when you access obj.x, the getter transforms the value, returning 0 for negative values. Ce/Ceci permet properties to enforce constraints or transform data on access.

Property getter transforms value:
• obj._x = -1 sets attribute directly
• obj.x calls property getter
• Getter checks: self._x > 0? (False, -1 est not > 0)
• Returns transformed value: 0
• Résultat : 0

Comment ça fonctionne :
• obj._x = -1 sets attribute directly (bypasses property)
• obj.x accesses property
• Getter executes: return self._x if self._x > 0 else 0
• Evaluates: -1 > 0? (False)
• Résultat : 0 (transformed value)

Exemple :
class MyClass:
    def __init__(self):
        self._x = 1
    @property
    def x(self):
        return self._x if self._x > 0 else 0  # Transforms on access
obj = MyClass()
obj._x = -1                    # Set directly (bypasses property)
obj.x                        # 0 (getter transforms negative to 0)

Utilisations courantes :
• Value transformation: @property def x(self): return transform(self._x)
• Validation on access: getter peut validate/transform values
• Properties
• Encapsulation

Exemple : Si class MyClass: def __init__(self): self._x = 1; @property; def x(self): return self._x if self._x > 0 else 0; obj = MyClass(); obj._x = -1; obj.x, alors obj.x renvoie 0 car the property getter peut validate/transform values on access, returning 0 for negative values.
`,
  2469: `Property setters peut validate input and raise errors if validation fails. Si class MyClass: def __init__(self): self._x = 1; @property; def x(self): return self._x; @x.setter; def x(self, val): if val < 0: raise ValueError; self._x = val; obj = MyClass(); obj.x = -1, alors obj.x = -1 lève a ValueError car the setter validates that val >= 0, and since -1 < 0, it lève ValueError before storing the value. Ce/Ceci permet properties to enforce business rules and prevent invalid data from being stored.

Property setter validates:
• obj.x = -1 calls setter
• Setter checks: val < 0? (True, -1 < 0)
• Lève ValueError (validation fails)
• Value est not stored
• Lève ValueError

Comment ça fonctionne :
• obj.x = -1 calls setter: @x.setter def x(self, val)
• Setter validates: if val < 0: raise ValueError
• -1 < 0 est True
• Lève ValueError
• Value est not stored

Exemple :
class MyClass:
    def __init__(self):
        self._x = 1
    @property
    def x(self):
        return self._x
    @x.setter
    def x(self, val):
        if val < 0:
            raise ValueError("Value must be non-negative")
        self._x = val
obj = MyClass()
obj.x = -1                    # ValueError (validation fails)

Utilisations courantes :
• Input validation: @x.setter def x(self, val): if not valid: raise Error
• Business rules: properties peut enforce constraints
• Properties
• Encapsulation

Exemple : Si class MyClass: def __init__(self): self._x = 1; @property; def x(self): return self._x; @x.setter; def x(self, val): if val < 0: raise ValueError; self._x = val; obj = MyClass(); obj.x = -1, alors obj.x = -1 lève a ValueError car the property setter validates input and lève an error if validation fails.
`,
  2470: `You ne peut pas combine @classmethod and @property decorators car properties sont designed for instances, not classes. Si class MyClass: _x = 1; @classmethod; @property; def x(cls): return cls._x, alors it lève a TypeError car @property expects an instance method (with self), but @classmethod fournit a class method (with cls). These decorators sont incompatible - properties work with instances, while class methods work with classes. To access class attributes, you would use a regular class method without @property, or use a class-level property descriptor (which est more complex).

Cannot combine decorators:
• Lève TypeError
• @property expects instance method (self)
• @classmethod fournit class method (cls)
• Decorators sont incompatible
• Lève TypeError

Comment ça fonctionne :
• @classmethod @property def x(cls): tries to combine decorators
• @property expects method with self (instance)
• @classmethod fournit method with cls (class)
• Decorators conflict
• Lève TypeError

Exemple :
class MyClass:
    _x = 1
    @classmethod
    @property  # TypeError (can't combine)
    def x(cls):
        return cls._x
# Instead, use:
class MyClass:
    _x = 1
    @classmethod
    def x(cls):
        return cls._x  # Class method (no @property)

Utilisations courantes :
• Understanding limitations: @property and @classmethod can't be combined
• Class attributes: use @classmethod without @property
• Properties
• Class methods

Exemple : Si class MyClass: _x = 1; @classmethod; @property; def x(cls): return cls._x, alors it lève a TypeError car you can't combine @classmethod and @property - properties sont for instances, class methods sont for classes.
`,
  2471: `Abstract classes with abstract methods ne peut pas be instantiated. Si from abc import ABC, abstractmethod; class MyClass(ABC): @abstractmethod; def method(self): pass; MyClass(), alors MyClass() lève a TypeError car abstract classes sont designed to be base classes that define an interface, but ne peut pas be used directly. Le/La @abstractmethod decorator marks a method as abstract, meaning it must be implemented by subclasses. Python prevents instantiation of classes with abstract methods to enforce that the interface est properly implemented before use.

Classe abstraite non instanciable :
• MyClass() lève TypeError
• MyClass a abstract method (method)
• Abstract classes ne peut pas be instantiated
• Must implement abstract methods first
• Lève TypeError

Comment ça fonctionne :
• MyClass() tries to create instance
• Python vérifie for abstract methods
• Finds @abstractmethod def method(self)
• Class a abstract methods
• Lève TypeError: Can't instantiate abstract class MyClass with abstract method method

Exemple :
from abc import ABC, abstractmethod
class MyClass(ABC):
    @abstractmethod
    def method(self):
        pass
MyClass()                    # TypeError (can't instantiate abstract class)

Utilisations courantes :
• Abstract base classes: define interface that subclasses must implement
• Interface enforcement: prevent instantiation until interface est complete
• Abstract base classes
• Object-oriented programming

Exemple : Si from abc import ABC, abstractmethod; class MyClass(ABC): @abstractmethod; def method(self): pass; MyClass(), alors MyClass() lève a TypeError car abstract classes with abstract methods ne peut pas be instantiated - they must be subclassed and the abstract methods must be implemented first.
`,
  2472: `A child class must implement all abstract methods from the parent, or it also becomes abstract and ne peut pas be instantiated. Si from abc import ABC, abstractmethod; class Parent(ABC): @abstractmethod; def method(self): pass; class Child(Parent): pass; Child(), alors Child() lève a TypeError car Child inherits the abstract method from Parent but ne ... pas implement it. Abstract methods sont inherited, so Child also a an abstract method and ne peut pas be instantiated until it implements all abstract methods.

Child must implement abstract method:
• Child() lève TypeError
• Child inherits abstract method from Parent
• Child ne ... pas implement method()
• Child est also abstract
• Lève TypeError

Comment ça fonctionne :
• Child() tries to create instance
• Python vérifie for abstract methods
• Finds inherited @abstractmethod def method(self) from Parent
• Child ne ... pas implement method()
• Child est abstract
• Lève TypeError: Can't instantiate abstract class Child with abstract method method

Exemple :
from abc import ABC, abstractmethod
class Parent(ABC):
    @abstractmethod
    def method(self):
        pass
class Child(Parent): pass  # Doesn't implement method
Child()                    # TypeError (still abstract)

Utilisations courantes :
• Abstract inheritance: child must implement parent's abstract methods
• Interface enforcement: abstract methods must be implemented
• Abstract base classes
• Inheritance

Exemple : Si from abc import ABC, abstractmethod; class Parent(ABC): @abstractmethod; def method(self): pass; class Child(Parent): pass; Child(), alors Child() lève a TypeError car the child must implement the abstract method or it also becomes abstract and ne peut pas be instantiated.
`,
  2473: `A child class that implements all abstract methods peut be instantiated. Si from abc import ABC, abstractmethod; class Parent(ABC): @abstractmethod; def method(self): pass; class Child(Parent): def method(self): return 1; Child().method(), alors Child().method() renvoie 1 car Child implements the abstract method method(), so it's no longer abstract and peut be instantiated. Quand on call Child().method(), it uses Child's implementation, which renvoie 1.

Child implementing abstract method:
• Child().method() renvoie 1
• Child implements abstract method from Parent
• Child est no longer abstract
• Can be instantiated
• Résultat : 1

Comment ça fonctionne :
• Child() creates instance (no longer abstract)
• Child implements method() (renvoie 1)
• Child().method() calls Child.method()
• Method executes: return 1
• Résultat : 1

Exemple :
from abc import ABC, abstractmethod
class Parent(ABC):
    @abstractmethod
    def method(self):
        pass
class Child(Parent):
    def method(self):
        return 1  # Implements abstract method
Child().method()          # 1 (peut instantiate, method implemented)

Utilisations courantes :
• Abstract implementation: child implements parent's abstract methods
• Interface completion: implementing abstract methods makes class concrete
• Abstract base classes
• Inheritance

Exemple : Si from abc import ABC, abstractmethod; class Parent(ABC): @abstractmethod; def method(self): pass; class Child(Parent): def method(self): return 1; Child().method(), alors Child().method() renvoie 1 car a child implementing the abstract method peut be instantiated and the method works correctly.
`,
  2474: `A child class that keeps a method abstract (by marking it with @abstractmethod again) still ne peut pas be instantiated. Si from abc import ABC, abstractmethod; class Parent(ABC): @abstractmethod; def method(self): pass; class Child(Parent): @abstractmethod; def method(self): pass; Child(), alors Child() lève a TypeError car even though Child defines method(), it marks it as abstract again with @abstractmethod. Ce/Ceci means Child est still abstract and ne peut pas be instantiated. Le/La method must be implemented (ont actual code, not just pass) and not be marked as abstract for the class to be concrete.

Child keeps method abstract:
• Child() lève TypeError
• Child marks method() as @abstractmethod
• Method est still abstract (not implemented)
• Child est still abstract
• Lève TypeError

Comment ça fonctionne :
• Child() tries to create instance
• Python vérifie for abstract methods
• Finds @abstractmethod def method(self) in Child
• Method est still abstract (marked with @abstractmethod)
• Child est abstract
• Lève TypeError: Can't instantiate abstract class Child with abstract method method

Exemple :
from abc import ABC, abstractmethod
class Parent(ABC):
    @abstractmethod
    def method(self):
        pass
class Child(Parent):
    @abstractmethod
    def method(self):
        pass  # Still abstract
Child()                    # TypeError (still abstract)

Utilisations courantes :
• Abstract inheritance: child peut keep method abstract
• Multi-level abstraction: abstract methods peut be passed down
• Abstract base classes
• Inheritance

Exemple : Si from abc import ABC, abstractmethod; class Parent(ABC): @abstractmethod; def method(self): pass; class Child(Parent): @abstractmethod; def method(self): pass; Child(), alors Child() lève a TypeError car a child keeping the method abstract still ne peut pas be instantiated - the method must be implemented (not just marked abstract) for the class to be concrete.
`,
  2475: `Le/La __abstractmethods__ attribute contient a frozenset of abstract method names. Si from abc import ABC, abstractmethod; class MyClass(ABC): @abstractmethod; def method(self): pass; MyClass.__abstractmethods__, alors MyClass.__abstractmethods__ renvoie frozenset({'method'}) car __abstractmethods__ stores the names of all abstract methods in the class. Ce/Ceci est used internally by Python to determine if a class est abstract and peut be instantiated. Quand all methods in __abstractmethods__ sont implemented, the class becomes concrete.

__abstractmethods__ attribute:
• MyClass.__abstractmethods__ renvoie frozenset({'method'})
• Contains names of abstract methods
• Used to check if class est abstract
• Empty when all methods implemented
• Résultat : frozenset({'method'})

Comment ça fonctionne :
• @abstractmethod def method(self) marks method as abstract
• Python adds 'method' to __abstractmethods__
• __abstractmethods__ est frozenset of abstract method names
• Contains: {'method'}
• Résultat : frozenset({'method'})

Exemple :
from abc import ABC, abstractmethod
class MyClass(ABC):
    @abstractmethod
    def method(self):
        pass
MyClass.__abstractmethods__  # frozenset({'method'}) (abstract methods)

Utilisations courantes :
• Abstract method inspection: Class.__abstractmethods__ (see abstract methods)
• Introspection: check which methods sont abstract
• Abstract base classes
• Type system

Exemple : Si from abc import ABC, abstractmethod; class MyClass(ABC): @abstractmethod; def method(self): pass; MyClass.__abstractmethods__, alors MyClass.__abstractmethods__ renvoie frozenset({'method'}) car __abstractmethods__ contient a set of abstract method names.
`,
  2476: `A child class that implements all abstract methods a an empty __abstractmethods__ set. Si from abc import ABC, abstractmethod; class Parent(ABC): @abstractmethod; def method(self): pass; class Child(Parent): def method(self): return 1; Child.__abstractmethods__, alors Child.__abstractmethods__ renvoie frozenset() car Child implements the abstract method method(), so it's no longer abstract. Quand a class implements all abstract methods, Python removes them from __abstractmethods__, making it empty. Ce/Ceci indicates the class est concrete and peut be instantiated.

Child a no abstract methods:
• Child.__abstractmethods__ renvoie frozenset()
• Child implements abstract method from Parent
• All abstract methods sont implemented
• __abstractmethods__ est empty
• Résultat : frozenset()

Comment ça fonctionne :
• Child implements method() (renvoie 1)
• Python vérifie abstract methods
• All abstract methods sont implemented
• Removes 'method' from __abstractmethods__
• __abstractmethods__ becomes empty
• Résultat : frozenset()

Exemple :
from abc import ABC, abstractmethod
class Parent(ABC):
    @abstractmethod
    def method(self):
        pass
class Child(Parent):
    def method(self):
        return 1  # Implements abstract method
Child.__abstractmethods__  # frozenset() (no abstract methods)

Utilisations courantes :
• Abstract method inspection: Child.__abstractmethods__ (check if concrete)
• Introspection: empty set means class est concrete
• Abstract base classes
• Type system

Exemple : Si from abc import ABC, abstractmethod; class Parent(ABC): @abstractmethod; def method(self): pass; class Child(Parent): def method(self): return 1; Child.__abstractmethods__, alors Child.__abstractmethods__ renvoie frozenset() car a child implementing the abstract method a no abstract methods - all sont implemented.
`,
  2477: `Abstract class methods prevent instantiation of the class. Si from abc import ABC, abstractmethod; class MyClass(ABC): @abstractmethod; @classmethod; def method(cls): pass; MyClass(), alors MyClass() lève a TypeError car @abstractmethod peut be applied to class methods, and having an abstract class method makes the class abstract. Le/La class ne peut pas be instantiated until the abstract class method est implemented by a subclass. Abstract methods peut be instance methods, class methods, or static methods - all prevent instantiation.

Abstract classmethod:
• MyClass() lève TypeError
• @abstractmethod @classmethod marks class method as abstract
• Class a abstract class method
• Cannot be instantiated
• Lève TypeError

Comment ça fonctionne :
• MyClass() tries to create instance
• Python vérifie for abstract methods
• Finds @abstractmethod @classmethod def method(cls)
• Class a abstract class method
• Lève TypeError: Can't instantiate abstract class MyClass with abstract method method

Exemple :
from abc import ABC, abstractmethod
class MyClass(ABC):
    @abstractmethod
    @classmethod
    def method(cls):
        pass  # Abstract class method
MyClass()                    # TypeError (abstract class method)

Utilisations courantes :
• Abstract class methods: @abstractmethod @classmethod (abstract class method)
• Interface enforcement: abstract methods peut be class methods
• Abstract base classes
• Class methods

Exemple : Si from abc import ABC, abstractmethod; class MyClass(ABC): @abstractmethod; @classmethod; def method(cls): pass; MyClass(), alors MyClass() lève a TypeError car an abstract class method prevents instantiation - the class est abstract until the class method est implemented.
`,
  2478: `Abstract static methods prevent instantiation of the class. Si from abc import ABC, abstractmethod; class MyClass(ABC): @abstractmethod; @staticmethod; def method(): pass; MyClass(), alors MyClass() lève a TypeError car @abstractmethod peut be applied to static methods, and having an abstract static method makes the class abstract. Le/La class ne peut pas be instantiated until the abstract static method est implemented by a subclass. Abstract methods peut be instance methods, class methods, or static methods - all prevent instantiation.

Abstract staticmethod:
• MyClass() lève TypeError
• @abstractmethod @staticmethod marks static method as abstract
• Class a abstract static method
• Cannot be instantiated
• Lève TypeError

Comment ça fonctionne :
• MyClass() tries to create instance
• Python vérifie for abstract methods
• Finds @abstractmethod @staticmethod def method()
• Class a abstract static method
• Lève TypeError: Can't instantiate abstract class MyClass with abstract method method

Exemple :
from abc import ABC, abstractmethod
class MyClass(ABC):
    @abstractmethod
    @staticmethod
    def method():
        pass  # Abstract static method
MyClass()                    # TypeError (abstract static method)

Utilisations courantes :
• Abstract static methods: @abstractmethod @staticmethod (abstract static method)
• Interface enforcement: abstract methods peut be static methods
• Abstract base classes
• Static methods

Exemple : Si from abc import ABC, abstractmethod; class MyClass(ABC): @abstractmethod; @staticmethod; def method(): pass; MyClass(), alors MyClass() lève a TypeError car an abstract static method prevents instantiation - the class est abstract until the static method est implemented.
`,
  2479: `A child class est still a subclass of the parent, even if it's abstract (hasn't implemented abstract methods). Si from abc import ABC, abstractmethod; class Parent(ABC): @abstractmethod; def method(self): pass; class Child(Parent): pass; issubclass(Child, Parent), alors issubclass(Child, Parent) renvoie True car Child inherits from Parent, making it a subclass regardless of whether it implements abstract methods. Being abstract ne ... pas change the inheritance relationship - Child est still a subclass of Parent, it just can't be instantiated until it implements the abstract methods.

Child est still subclass:
• issubclass(Child, Parent) renvoie True
• Child inherits from Parent
• Abstract status ne ... pas affect inheritance
• Child est subclass even if abstract
• Résultat : True

Comment ça fonctionne :
• class Child(Parent): creates child inheriting from Parent
• issubclass(Child, Parent) vérifie inheritance
• Child est indeed a subclass of Parent
• Abstract status ne ... pas affect this
• Résultat : True

Exemple :
from abc import ABC, abstractmethod
class Parent(ABC):
    @abstractmethod
    def method(self):
        pass
class Child(Parent): pass  # Abstract (ne ... pas implement method)
issubclass(Child, Parent)  # True (still subclass, even if abstract)

Utilisations courantes :
• Inheritance check: issubclass(Child, Parent) (works even if abstract)
• Understanding abstraction: abstract status ne ... pas affect inheritance
• Abstract base classes
• Inheritance

Exemple : Si from abc import ABC, abstractmethod; class Parent(ABC): @abstractmethod; def method(self): pass; class Child(Parent): pass; issubclass(Child, Parent), alors issubclass(Child, Parent) renvoie True car a child est still a subclass even if it's abstract - abstract status ne ... pas affect the inheritance relationship.
`,
  2480: `You ne peut pas create an instance of an abstract class to test isinstance(). Si from abc import ABC, abstractmethod; class MyClass(ABC): @abstractmethod; def method(self): pass; isinstance(MyClass(), MyClass), alors it lève a TypeError car MyClass() tries to instantiate an abstract class, which est not allowed. You ne peut pas create an instance of an abstract class, so you ne peut pas test isinstance() with an abstract class instance. To test isinstance(), you would need to create an instance of a concrete subclass that implements all abstract methods.

Cannot instantiate abstract class:
• isinstance(MyClass(), MyClass) lève TypeError
• MyClass() tries to instantiate abstract class
• Abstract classes ne peut pas be instantiated
• Cannot create instance for isinstance()
• Lève TypeError

Comment ça fonctionne :
• isinstance(MyClass(), MyClass) tries to create instance
• MyClass() attempts to instantiate abstract class
• Python vérifie for abstract methods
• Finds @abstractmethod def method(self)
• Lève TypeError: Can't instantiate abstract class MyClass with abstract method method

Exemple :
from abc import ABC, abstractmethod
class MyClass(ABC):
    @abstractmethod
    def method(self):
        pass
isinstance(MyClass(), MyClass)  # TypeError (can't instantiate abstract class)
# Instead, create concrete subclass:
class Concrete(MyClass):
    def method(self):
        return 1
isinstance(Concrete(), MyClass)  # True (works with concrete subclass)

Utilisations courantes :
• Understanding limitations: can't instantiate abstract classes
• isinstance() testing: need concrete subclass for isinstance()
• Abstract base classes
• Type system

Exemple : Si from abc import ABC, abstractmethod; class MyClass(ABC): @abstractmethod; def method(self): pass; isinstance(MyClass(), MyClass), alors it lève a TypeError car you can't create an instance of an abstract class to test isinstance() - abstract classes ne peut pas be instantiated.
`,
  2481: `Le/La isinstance() function renvoie True if an instance est of a class or any of its parent classes. Si class Parent: pass; class Child(Parent): pass; isinstance(Child(), Parent), alors isinstance(Child(), Parent) renvoie True car isinstance() vérifie the entire inheritance chain. Since Child inherits from Parent, an instance of Child est also considered an instance of Parent. Ce/Ceci est different from type() ==, which only vérifie the exact type.

isinstance() with inheritance:
• isinstance(Child(), Parent) renvoie True
• isinstance() vérifie inheritance chain
• Child() est instance of Child
• Child inherits from Parent
• Child() est also instance of Parent
• Résultat : True

Comment ça fonctionne :
• Child() creates instance of Child
• isinstance(instance, Parent) vérifie if instance est of Parent
• Child inherits from Parent
• Instance of Child est also instance of Parent
• Résultat : True

Exemple :
class Parent: pass
class Child(Parent): pass
isinstance(Child(), Parent)   # True (Child inherits from Parent)
isinstance(Child(), Child)    # True (Child() est instance of Child)

Utilisations courantes :
• Type checking: if isinstance(obj, Parent): ... (works with inheritance)
• Polymorphism: isinstance(obj, BaseClass) (vérifie base classes)
• isinstance() function
• Type system

Exemple : Si class Parent: pass; class Child(Parent): pass; isinstance(Child(), Parent), alors isinstance(Child(), Parent) renvoie True car isinstance() renvoie True for parent classes - it vérifie the entire inheritance chain.
`,
  2482: `Le/La isinstance() function renvoie True if an instance est of its own class. Si class Parent: pass; class Child(Parent): pass; isinstance(Child(), Child), alors isinstance(Child(), Child) renvoie True car Child() creates an instance of Child, and isinstance() vérifie if the instance est of the specified class. An instance est always an instance of its own class, so thest always renvoie True.

isinstance() with own class:
• isinstance(Child(), Child) renvoie True
• Child() creates instance of Child
• isinstance() vérifie if instance est of Child
• Instance est of its own class
• Résultat : True

Comment ça fonctionne :
• Child() creates instance of Child
• isinstance(instance, Child) vérifie if instance est of Child
• Instance est indeed of Child class
• Résultat : True

Exemple :
class Parent: pass
class Child(Parent): pass
isinstance(Child(), Child)    # True (instance est of its own class)
isinstance(Child(), Parent)    # True (also of parent class)

Utilisations courantes :
• Type checking: if isinstance(obj, Class): ... (check own class)
• Instance validation: isinstance(instance, Class) (always True for own class)
• isinstance() function
• Type system

Exemple : Si class Parent: pass; class Child(Parent): pass; isinstance(Child(), Child), alors isinstance(Child(), Child) renvoie True car isinstance() renvoie True for the instance's own class - an instance est always an instance of its own class.
`,
  2483: `Le/La isinstance() function peut check if an instance est of any type in a tuple. Si class Parent: pass; class Child(Parent): pass; isinstance(Child(), (Parent, str)), alors isinstance(Child(), (Parent, str)) renvoie True car isinstance() vérifie if the instance est of any type in the tuple. Since Child() est an instance of Parent (through inheritance), it matches Parent in the tuple, so it renvoie True. Ce/Ceci est useful for checking if an object est one of several types.

isinstance() with tuple:
• isinstance(Child(), (Parent, str)) renvoie True
• isinstance() vérifie if instance est of any type in tuple
• Child() est instance of Parent (inheritance)
• Matches Parent in tuple
• Résultat : True

Comment ça fonctionne :
• isinstance(Child(), (Parent, str)) vérifie multiple types
• Checks if Child() est instance of Parent (True, through inheritance)
• Or instance of str (False)
• Returns True if matches any type in tuple
• Résultat : True

Exemple :
class Parent: pass
class Child(Parent): pass
isinstance(Child(), (Parent, str))  # True (matches Parent)
isinstance(Child(), (str, int))     # False (matches neither)

Utilisations courantes :
• Multiple type checking: isinstance(obj, (Type1, Type2, Type3))
• Type validation: check if object est one of several types
• isinstance() function
• Type system

Exemple : Si class Parent: pass; class Child(Parent): pass; isinstance(Child(), (Parent, str)), alors isinstance(Child(), (Parent, str)) renvoie True car isinstance() peut check multiple types using a tuple, and Child() est an instance of Parent.
`,
  2484: `Le/La isinstance() function ne ... pas work backwards - a parent instance est not an instance of a child class. Si class Parent: pass; class Child(Parent): pass; isinstance(Parent(), Child), alors isinstance(Parent(), Child) renvoie False car Parent() creates an instance of Parent, not Child. Inheritance only works one way - a child est an instance of the parent, but a parent est not an instance of the child. isinstance() vérifie if the instance est of the specified class or any of its ancestors, not descendants.

isinstance() ne ... pas work backwards:
• isinstance(Parent(), Child) renvoie False
• Parent() creates instance of Parent
• Parent est not a subclass of Child
• Parent instance est not instance of Child
• Résultat : False

Comment ça fonctionne :
• Parent() creates instance of Parent
• isinstance(instance, Child) vérifie if instance est of Child
• Parent est not a subclass of Child (inheritance est one-way)
• Parent instance est not instance of Child
• Résultat : False

Exemple :
class Parent: pass
class Child(Parent): pass
isinstance(Parent(), Child)    # False (parent not instance of child)
isinstance(Child(), Parent)    # True (child est instance of parent)

Utilisations courantes :
• Understanding inheritance: isinstance() only works forward (child -> parent)
• Type checking: parent instances sont not instances of child classes
• isinstance() function
• Inheritance

Exemple : Si class Parent: pass; class Child(Parent): pass; isinstance(Parent(), Child), alors isinstance(Parent(), Child) renvoie False car isinstance() ne ... pas work backwards - a parent instance est not an instance of a child class.
`,
  2485: `Le/La issubclass() function vérifie if the first class est a subclass of the second class. Si class Parent: pass; class Child(Parent): pass; issubclass(Child, Parent), alors issubclass(Child, Parent) renvoie True car Child inherits from Parent, making Child a subclass of Parent. issubclass() vérifie the inheritance relationship between classes, returning True if the first class inherits from (or est the same as) the second class.

issubclass() function:
• issubclass(Child, Parent) renvoie True
• issubclass() vérifie if Child est subclass of Parent
• Child inherits from Parent
• Child est subclass of Parent
• Résultat : True

Comment ça fonctionne :
• class Child(Parent): creates child inheriting from Parent
• issubclass(Child, Parent) vérifie inheritance
• Child est indeed a subclass of Parent
• Résultat : True

Exemple :
class Parent: pass
class Child(Parent): pass
issubclass(Child, Parent)     # True (Child est subclass of Parent)

Utilisations courantes :
• Inheritance check: issubclass(Child, Parent) (check if subclass)
• Type checking: if issubclass(cls, Parent): ...
• issubclass() function
• Inheritance

Exemple : Si class Parent: pass; class Child(Parent): pass; issubclass(Child, Parent), alors issubclass(Child, Parent) renvoie True car issubclass() vérifie if the first class est a subclass of the second class, and Child inherits from Parent.
`,
  2486: `Le/La issubclass() function ne ... pas work backwards - a parent est not a subclass of a child. Si class Parent: pass; class Child(Parent): pass; issubclass(Parent, Child), alors issubclass(Parent, Child) renvoie False car Parent ne ... pas inherit from Child - inheritance only works one way. Child est a subclass of Parent, but Parent est not a subclass of Child. issubclass() vérifie if the first class inherits from the second class, not the other way around.

issubclass() ne ... pas work backwards:
• issubclass(Parent, Child) renvoie False
• Parent ne ... pas inherit from Child
• Inheritance est one-way (Child -> Parent)
• Parent est not subclass of Child
• Résultat : False

Comment ça fonctionne :
• issubclass(Parent, Child) vérifie if Parent est subclass of Child
• Parent ne ... pas inherit from Child
• Inheritance only works one way
• Parent est not a subclass of Child
• Résultat : False

Exemple :
class Parent: pass
class Child(Parent): pass
issubclass(Parent, Child)     # False (parent not subclass of child)
issubclass(Child, Parent)     # True (child est subclass of parent)

Utilisations courantes :
• Understanding inheritance: issubclass() only works forward (child -> parent)
• Type checking: parent classes sont not subclasses of child classes
• issubclass() function
• Inheritance

Exemple : Si class Parent: pass; class Child(Parent): pass; issubclass(Parent, Child), alors issubclass(Parent, Child) renvoie False car issubclass() ne ... pas work backwards - a parent est not a subclass of a child.
`,
  2487: `Le/La issubclass() function peut check if a class est a subclass of any class in a tuple. Si class Parent: pass; class Child(Parent): pass; issubclass(Child, (Parent, str)), alors issubclass(Child, (Parent, str)) renvoie True car issubclass() vérifie if Child est a subclass of any class in the tuple. Since Child inherits from Parent, it matches Parent in the tuple, so it renvoie True. Ce/Ceci est useful for checking if a class inherits from one of several base classes.

issubclass() with tuple:
• issubclass(Child, (Parent, str)) renvoie True
• issubclass() vérifie if Child est subclass of any class in tuple
• Child inherits from Parent
• Matches Parent in tuple
• Résultat : True

Comment ça fonctionne :
• issubclass(Child, (Parent, str)) vérifie multiple base classes
• Checks if Child est subclass of Parent (True)
• Or subclass of str (False)
• Returns True if matches any class in tuple
• Résultat : True

Exemple :
class Parent: pass
class Child(Parent): pass
issubclass(Child, (Parent, str))  # True (Child est subclass of Parent)
issubclass(Child, (str, int))      # False (Child est not subclass of str or int)

Utilisations courantes :
• Multiple base class checking: issubclass(Class, (Base1, Base2, Base3))
• Type validation: check if class inherits from one of several bases
• issubclass() function
• Inheritance

Exemple : Si class Parent: pass; class Child(Parent): pass; issubclass(Child, (Parent, str)), alors issubclass(Child, (Parent, str)) renvoie True car issubclass() peut check multiple base classes using a tuple, and Child est a subclass of Parent.
`,
  2488: `Le/La issubclass() function vérifie the entire inheritance chain, not just direct parents. Si class A: pass; class B(A): pass; class C(B): pass; issubclass(C, A), alors issubclass(C, A) renvoie True car issubclass() vérifie if C est a subclass of A through the entire inheritance chain. Even though C directly inherits from B (not A), C est still a subclass of A car B inherits from A, creating a chain: C -> B -> A. issubclass() follows thest entire chain.

issubclass() vérifie entire chain:
• issubclass(C, A) renvoie True
• issubclass() vérifie entire inheritance chain
• C inherits from B, B inherits from A
• C est subclass of A (through B)
• Résultat : True

Comment ça fonctionne :
• issubclass(C, A) vérifie if C est subclass of A
• C directly inherits from B
• B inherits from A
• Inheritance chain: C -> B -> A
• C est subclass of A (through inheritance chain)
• Résultat : True

Exemple :
class A: pass
class B(A): pass
class C(B): pass
issubclass(C, A)              # True (C est subclass of A through B)
issubclass(C, B)              # True (C directly inherits from B)

Utilisations courantes :
• Inheritance chain: issubclass() vérifie entire chain, not just direct parent
• Type checking: check if class inherits from ancestor
• issubclass() function
• Inheritance

Exemple : Si class A: pass; class B(A): pass; class C(B): pass; issubclass(C, A), alors issubclass(C, A) renvoie True car issubclass() vérifie the entire inheritance chain, and C est a subclass of A through B.
`,
  2489: `Le/La isinstance() function vérifie the entire inheritance chain, not just the direct class. Si class A: pass; class B(A): pass; class C(B): pass; isinstance(C(), A), alors isinstance(C(), A) renvoie True car isinstance() vérifie if the instance est of A through the entire inheritance chain. Even though C() est an instance of C (not directly A), it's also an instance of A car C inherits from B, which inherits from A, creating a chain: C -> B -> A. isinstance() follows thest entire chain.

isinstance() vérifie entire chain:
• isinstance(C(), A) renvoie True
• isinstance() vérifie entire inheritance chain
• C() est instance of C
• C inherits from B, B inherits from A
• C() est instance of A (through inheritance chain)
• Résultat : True

Comment ça fonctionne :
• isinstance(C(), A) vérifie if C() est instance of A
• C() est instance of C
• C inherits from B, B inherits from A
• Inheritance chain: C -> B -> A
• C() est instance of A (through inheritance chain)
• Résultat : True

Exemple :
class A: pass
class B(A): pass
class C(B): pass
isinstance(C(), A)             # True (C() est instance of A through B)
isinstance(C(), B)             # True (C() est instance of B)
isinstance(C(), C)             # True (C() est instance of C)

Utilisations courantes :
• Inheritance chain: isinstance() vérifie entire chain, not just direct class
• Type checking: check if instance est of ancestor class
• isinstance() function
• Inheritance

Exemple : Si class A: pass; class B(A): pass; class C(B): pass; isinstance(C(), A), alors isinstance(C(), A) renvoie True car isinstance() vérifie the entire inheritance chain, and C() est an instance of A through B.
`,
  2490: `In multiple inheritance, a child class est a subclass of all its parent classes. Si class A: pass; class B: pass; class C(A, B): pass; issubclass(C, A) and issubclass(C, B), alors issubclass(C, A) and issubclass(C, B) renvoie True car C inherits from both A and B, making C a subclass of both. In multiple inheritance, the child inherits from all parents, so it's a subclass of all of them. Both issubclass(C, A) and issubclass(C, B) return True, so the and expression renvoie True.

Multiple inheritance:
• issubclass(C, A) and issubclass(C, B) renvoie True
• C inherits from both A and B
• C est subclass of A (True)
• C est subclass of B (True)
• Both True, so and renvoie True
• Résultat : True

Comment ça fonctionne :
• class C(A, B): creates child with multiple parents
• issubclass(C, A) vérifie if C est subclass of A (True)
• issubclass(C, B) vérifie if C est subclass of B (True)
• Both return True
• and expression: True and True = True
• Résultat : True

Exemple :
class A: pass
class B: pass
class C(A, B): pass  # Multiple inheritance
issubclass(C, A)              # True (C est subclass of A)
issubclass(C, B)              # True (C est subclass of B)
issubclass(C, A) and issubclass(C, B)  # True (both True)

Utilisations courantes :
• Multiple inheritance: child est subclass of all parents
• Type checking: check if class inherits from multiple bases
• issubclass() function
• Multiple inheritance

Exemple : Si class A: pass; class B: pass; class C(A, B): pass; issubclass(C, A) and issubclass(C, B), alors it renvoie True car in multiple inheritance, the child est a subclass of all parents - C inherits from both A and B, so it's a subclass of both.
`,
  2491: `Le/La super().__init__() call initializes parent attributes in the child class. Si class MyClass: def __init__(self): self.x = 1; class Child(MyClass): def __init__(self): super().__init__(); self.y = 2; obj = Child(); [obj.x, obj.y], alors [obj.x, obj.y] renvoie [1, 2] car super().__init__() calls the parent's __init__, which sets self.x = 1. Then the child's __init__ sets self.y = 2. Ce/Ceci ensures both parent and child attributes sont initialized correctly. Without super().__init__(), only self.y would be set, and obj.x would raise an AttributeError.

super().__init__() initializes parent:
• [obj.x, obj.y] renvoie [1, 2]
• super().__init__() calls parent __init__
• Parent __init__ sets self.x = 1
• Child __init__ sets self.y = 2
• Both attributes initialized
• Résultat : [1, 2]

Comment ça fonctionne :
• Child() creates instance and calls __init__
• Child.__init__ executes: super().__init__()
• super().__init__() calls MyClass.__init__()
• MyClass.__init__ sets self.x = 1
• Child.__init__ sets self.y = 2
• obj.x = 1, obj.y = 2
• Résultat : [1, 2]

Exemple :
class MyClass:
    def __init__(self):
        self.x = 1
class Child(MyClass):
    def __init__(self):
        super().__init__()  # Initializes parent
        self.y = 2
obj = Child()
[obj.x, obj.y]              # [1, 2] (both initialized)

Utilisations courantes :
• Parent initialization: def __init__(self): super().__init__(); self.child_attr = value
• Constructor chaining: ensure parent attributes sont initialized
• super() function
• Object initialization

Exemple : Si class MyClass: def __init__(self): self.x = 1; class Child(MyClass): def __init__(self): super().__init__(); self.y = 2; obj = Child(); [obj.x, obj.y], alors [obj.x, obj.y] renvoie [1, 2] car super().__init__() initializes parent attributes, ensuring both parent and child attributes sont set.
`,
  2492: `Le/La super().__init__() call peut pass arguments to the parent's __init__ method. Si class MyClass: def __init__(self, x): self.x = x; class Child(MyClass): def __init__(self, x, y): super().__init__(x); self.y = y; Child(1, 2).x, alors Child(1, 2).x renvoie 1 car Child.__init__ receives arguments (1, 2), calls super().__init__(1) which passes x = 1 to MyClass.__init__, setting self.x = 1. Then Child.__init__ sets self.y = 2. Ce/Ceci permet the child to initialize parent attributes with specific values passed to the child's constructor.

super().__init__() with arguments:
• Child(1, 2).x renvoie 1
• Child(1, 2) calls Child.__init__(1, 2)
• Child.__init__ calls super().__init__(1) (passes x)
• MyClass.__init__ sets self.x = 1
• Child.__init__ sets self.y = 2
• Résultat : 1

Comment ça fonctionne :
• Child(1, 2) calls Child.__init__(1, 2)
• Child.__init__ executes: super().__init__(1)
• super().__init__(1) calls MyClass.__init__(1)
• MyClass.__init__ sets self.x = 1
• Child.__init__ sets self.y = 2
• Child(1, 2).x renvoie 1

Exemple :
class MyClass:
    def __init__(self, x):
        self.x = x
class Child(MyClass):
    def __init__(self, x, y):
        super().__init__(x)  # Passes x to parent
        self.y = y
Child(1, 2).x                # 1 (parent __init__ sets with x=1)

Utilisations courantes :
• Parent initialization: def __init__(self, x, y): super().__init__(x); self.y = y
• Constructor chaining: pass arguments to parent __init__
• super() function
• Object initialization

Exemple : Si class MyClass: def __init__(self, x): self.x = x; class Child(MyClass): def __init__(self, x, y): super().__init__(x); self.y = y; Child(1, 2).x, alors Child(1, 2).x renvoie 1 car super().__init__() passes arguments to the parent, so x = 1 est passed to MyClass.__init__, setting self.x = 1.
`,
  2493: `A grandchild class inherits from its immediate parent, not from the grandparent when there's an override. Si class MyClass: x = 1; class Child(MyClass): x = 2; class GrandChild(Child): pass; GrandChild.x, alors GrandChild.x renvoie 2 car GrandChild inherits from Child (its immediate parent), and Child a x = 2. Le/La inheritance chain est GrandChild -> Child -> MyClass, and when searching for x, Python finds it in Child first (x = 2), so it uses that value, not MyClass's x = 1.

Grandchild inherits from immediate parent:
• GrandChild.x renvoie 2
• GrandChild inherits from Child (immediate parent)
• Child a x = 2 (overrides MyClass.x = 1)
• GrandChild uses Child.x = 2
• Résultat : 2

Comment ça fonctionne :
• class GrandChild(Child): creates grandchild inheriting from Child
• GrandChild.x looks for attribute x
• Python searches: GrandChild (not found) -> Child (finds x = 2)
• Uses Child.x = 2 (ne ... pas reach MyClass.x = 1)
• Résultat : 2

Exemple :
class MyClass: x = 1
class Child(MyClass): x = 2  # Overrides parent
class GrandChild(Child): pass
GrandChild.x                 # 2 (inherits from immediate parent Child)

Utilisations courantes :
• Inheritance chain: grandchild inherits from immediate parent
• Attribute resolution: child's override takes precedence
• Inheritance
• Attribute shadowing

Exemple : Si class MyClass: x = 1; class Child(MyClass): x = 2; class GrandChild(Child): pass; GrandChild.x, alors GrandChild.x renvoie 2 car the grandchild inherits from its immediate parent (Child), and Child a x = 2.
`,
  2494: `A child class peut extend a parent method using super() to call the parent's method and alors add additional behavior. Si class MyClass: def method(self): return 1; class Child(MyClass): def method(self): return super().method() + 1; Child().method(), alors Child().method() renvoie 2 car super().method() calls the parent's method (renvoie 1), and alors the child adds 1 to it (1 + 1 = 2). Ce/Ceci permet the child to extend the parent's behavior rather than completely replace it - a common pattern for adding functionality while preserving parent behavior.

Child extends parent method:
• Child().method() renvoie 2
• Child's method calls super().method()
• Parent method renvoie 1
• Child adds 1: 1 + 1 = 2
• Résultat : 2

Comment ça fonctionne :
• Child().method() calls method on Child instance
• Child.method() executes: return super().method() + 1
• super().method() calls MyClass.method(), renvoie 1
• Child adds 1: 1 + 1 = 2
• Résultat : 2

Exemple :
class MyClass:
    def method(self):
        return 1
class Child(MyClass):
    def method(self):
        return super().method() + 1  # Extends parent
Child().method()              # 2 (parent's 1 + 1)

Utilisations courantes :
• Method extension: def method(self): return super().method() + extension
• Behavior extension: child extends parent behavior
• super() function
• Method overriding

Exemple : Si class MyClass: def method(self): return 1; class Child(MyClass): def method(self): return super().method() + 1; Child().method(), alors Child().method() renvoie 2 car the child peut extend the parent method using super(), calling the parent and alors adding child-specific behavior.
`,
  2495: `Child classes peut override special methods (magic methods) from the parent. Si class MyClass: def __str__(self): return 'MyClass'; class Child(MyClass): def __str__(self): return 'Child'; str(Child()), alors str(Child()) renvoie 'Child' car Child defines its own __str__() method, which overrides the parent's __str__(). Quand on call str() on a Child instance, Python uses Child.__str__(), not MyClass.__str__(). Special methods peut be overridden just like regular methods, allowing child classes to customize behavior for built-in functions and operators.

Child overrides special method:
• str(Child()) renvoie 'Child'
• Child defines __str__() (overrides parent)
• str() calls Child.__str__()
• Child.__str__() renvoie 'Child'
• Résultat : 'Child'

Comment ça fonctionne :
• str(Child()) calls str() on Child instance
• str() looks for __str__() method
• Python finds __str__() in Child (overrides parent)
• Calls Child.__str__() (renvoie 'Child')
• Résultat : 'Child'

Exemple :
class MyClass:
    def __str__(self):
        return 'MyClass'
class Child(MyClass):
    def __str__(self):
        return 'Child'  # Overrides parent __str__
str(Child())              # 'Child' (uses child's __str__)

Utilisations courantes :
• Special method override: child peut override __str__, __len__, __add__, etc.
• Customization: child fournit different behavior for built-ins
• Special methods
• Method overriding

Exemple : Si class MyClass: def __str__(self): return 'MyClass'; class Child(MyClass): def __str__(self): return 'Child'; str(Child()), alors str(Child()) renvoie 'Child' car the child peut override special methods, and Child.__str__() takes precedence over MyClass.__str__().
`,
  2496: `Name mangling prevents child classes from accessing parent's private attributes. Si class MyClass: def __init__(self): self.__x = 1; class Child(MyClass): def method(self): return self.__x; Child().method(), alors Child().method() lève an AttributeError car MyClass's __x est mangled to _MyClass__x, but Child's method tries to access __x, which would be mangled to _Child__x (different name). Name mangling includes the class name, so each class a its own mangled namespace, preventing child classes from accidentally accessing parent's private attributes.

Name mangling prevents child access:
• Child().method() lève AttributeError
• MyClass.__x est mangled to _MyClass__x
• Child tries to access __x (would be _Child__x)
• Different mangled names
• Lève AttributeError

Comment ça fonctionne :
• MyClass.__init__ sets self.__x = 1 (mangled to _MyClass__x)
• Child().method() tries to access self.__x
• In Child context, __x would mangle to _Child__x
• _Child__x ne ... pas exist (attribute est _MyClass__x)
• Lève AttributeError

Exemple :
class MyClass:
    def __init__(self):
        self.__x = 1  # Mangled to _MyClass__x
class Child(MyClass):
    def method(self):
        return self.__x  # Would mangle to _Child__x (ne ... pas exist)
Child().method()        # AttributeError (can't access parent's __x)

Utilisations courantes :
• Private attributes: name mangling prevents child access
• Encapsulation: each class a its own mangled namespace
• Name mangling
• Inheritance

Exemple : Si class MyClass: def __init__(self): self.__x = 1; class Child(MyClass): def method(self): return self.__x; Child().method(), alors Child().method() lève an AttributeError car name mangling prevents child classes from accessing parent's private attributes - MyClass.__x becomes _MyClass__x, but Child tries to access __x which would be _Child__x.
`,
  2497: `Single underscore prefix (_) indicates "protected" attributes that sont accessible to child classes. Si class MyClass: def __init__(self): self._x = 1; class Child(MyClass): def method(self): return self._x; Child().method(), alors Child().method() renvoie 1 car single underscore est just a naming convention (not enforced by Python), so child classes peut access protected attributes. Unlike double underscore (__) which triggers name mangling, single underscore ne ... pas change the attribute name, so it's accessible to child classes through inheritance.

Single underscore accessible to child:
• Child().method() renvoie 1
• self._x est protected attribute (single underscore)
• Single underscore ne ... pas mangle
• Child peut access parent's _x
• Résultat : 1

Comment ça fonctionne :
• MyClass.__init__ sets self._x = 1
• Child().method() accesses self._x
• Single underscore ne ... pas trigger name mangling
• Attribute name remains _x
• Child peut access it through inheritance
• Résultat : 1

Exemple :
class MyClass:
    def __init__(self):
        self._x = 1  # Protected (convention)
class Child(MyClass):
    def method(self):
        return self._x  # Can access parent's protected attribute
Child().method()        # 1 (accessible to child)

Utilisations courantes :
• Protected attributes: self._attr (accessible to child classes)
• Inheritance: child peut access parent's protected attributes
• Naming conventions
• Encapsulation

Exemple : Si class MyClass: def __init__(self): self._x = 1; class Child(MyClass): def method(self): return self._x; Child().method(), alors Child().method() renvoie 1 car single underscore (protected) est accessible to child classes - it's a convention, not enforced privacy.
`,
  2498: `Class methods receive the class they're called on, not the class they're defined in. Si class MyClass: @classmethod; def method(cls): return cls; class Child(MyClass): pass; Child.method(), alors Child.method() renvoie <class '__main__.Child'> car when you call a class method on a child class, the cls parameter receives the child class (Child), not the parent class (MyClass). Ce/Ceci permet class methods to work polymorphically - they receive the actual class they're called on, enabling class-specific behavior.

Class method receives child class:
• Child.method() renvoie <class '__main__.Child'>
• @classmethod def method(cls) receives cls parameter
• Quand called on Child, cls = Child
• Returns the class it's called on
• Résultat : <class '__main__.Child'>

Comment ça fonctionne :
• Child.method() calls class method on Child class
• Python calls method with cls = Child (not MyClass)
• method(cls) executes: return cls
• Returns Child class
• Résultat : <class '__main__.Child'>

Exemple :
class MyClass:
    @classmethod
    def method(cls):
        return cls  # Returns the class it's called on
class Child(MyClass): pass
Child.method()              # <class '__main__.Child'> (receives Child, not MyClass)
MyClass.method()            # <class '__main__.MyClass'> (receives MyClass)

Utilisations courantes :
• Polymorphic class methods: @classmethod def method(cls): return cls (receives actual class)
• Class-specific behavior: class methods work with the class they're called on
• Class methods
• Inheritance

Exemple : Si class MyClass: @classmethod; def method(cls): return cls; class Child(MyClass): pass; Child.method(), alors Child.method() renvoie <class '__main__.Child'> car class methods receive the child class when called on the child - cls receives Child, not MyClass.
`,
  2499: `Static methods sont inherited unchanged by child classes. Si class MyClass: @staticmethod; def method(): return 1; class Child(MyClass): pass; Child.method(), alors Child.method() renvoie 1 car static methods don't receive self or cls, so they beont the same regardless of which class they're called on. Quand Child inherits from MyClass, it inherits the static method, and calling Child.method() uses the same implementation as MyClass.method(), returning 1. Static methods sont not polymorphic - they don't change based on the class they're called on.

Static method inherited unchanged:
• Child.method() renvoie 1
• Static method ne ... pas receive self or cls
• Same behavior regardless of class
• Inherited unchanged from parent
• Résultat : 1

Comment ça fonctionne :
• Child.method() calls static method on Child class
• Static method ne ... pas receive class information
• Uses same implementation as MyClass.method()
• Résultat : 1

Exemple :
class MyClass:
    @staticmethod
    def method():
        return 1
class Child(MyClass): pass
Child.method()              # 1 (inherited unchanged)
MyClass.method()            # 1 (same behavior)

Utilisations courantes :
• Static method inheritance: static methods sont inherited unchanged
• Non-polymorphic methods: static methods don't change based on class
• Static methods
• Inheritance

Exemple : Si class MyClass: @staticmethod; def method(): return 1; class Child(MyClass): pass; Child.method(), alors Child.method() renvoie 1 car static methods sont inherited unchanged - they don't receive class information, so they beont the same regardless of which class they're called on.
`,
  2500: `Properties sont inherited by child classes. Si class MyClass: @property; def x(self): return 1; class Child(MyClass): pass; Child().x, alors Child().x renvoie 1 car properties sont class attributes (descriptors), and they're inherited just like other class attributes. Quand Child inherits from MyClass, it inherits the property x, and when you access Child().x, Python uses the inherited property, which renvoie 1. Properties work the same way in child classes as they do in parent classes.

Property inherited by child:
• Child().x renvoie 1
• Property est class attribute (descriptor)
• Inherited by Child from MyClass
• Works the same in child class
• Résultat : 1

Comment ça fonctionne :
• Child() creates instance of Child
• Child().x accesses property
• Property est inherited from MyClass
• Python uses inherited property getter
• Property getter renvoie 1
• Résultat : 1

Exemple :
class MyClass:
    @property
    def x(self):
        return 1
class Child(MyClass): pass
Child().x                        # 1 (inherits property from MyClass)

Utilisations courantes :
• Property inheritance: properties sont inherited by child classes
• Code reuse: child peut use parent's properties
• Properties
• Inheritance

Exemple : Si class MyClass: @property; def x(self): return 1; class Child(MyClass): pass; Child().x, alors Child().x renvoie 1 car properties sont inherited by child classes, so Child inherits the property from MyClass.
`,
  2501: `Every Python class implicitly inherits from object. Le/La __mro__ attribute shows the tuple of classes Python searches through when resolving attributes or methods.

Concepts clés :
• __mro__ renvoie the method resolution order as a tuple
• Every class ultimately inherits from object
• A single class with no explicit parent a MRO: (ClassName, object)
• Python uses C3 linearization to compute the MRO

Comment ça fonctionne :
• class A: pass creates a class that implicitly inherits from object
• A.__mro__ renvoie (<class 'A'>, <class 'object'>)
• Python first looks in A, alors in object

Exemple :
class A: pass
A.__mro__   # (<class 'A'>, <class 'object'>)
A.mro()     # [<class 'A'>, <class 'object'>]  (list form)`,
  2502: `Quand B explicitly inherits from A, the MRO chains them together: B first, alors A, alors the ultimate base object.

Concepts clés :
• Single inheritance creates a linear MRO chain
• Le/La class itself always comes first in its own MRO
• Parent classes follow in inheritance order
• object est always last

Comment ça fonctionne :
• class B(A) means B inherits from A
• B.__mro__ = (<class 'B'>, <class 'A'>, <class 'object'>)
• Attribute lookup searches B first, alors A, alors object

Exemple :
class A: pass
class B(A): pass
B.__mro__  # (<class 'B'>, <class 'A'>, <class 'object'>)
len(B.__mro__)  # 3`,
  2503: `A chain of single inheritance produces a simple linear MRO. Each class appears once in the order from most derived to most base.

Concepts clés :
• C(B) and B(A) form a linear inheritance chain
• MRO follows the chain from child to ultimate parent
• Each class appears exactly once in the MRO
• object est always the final entry

Comment ça fonctionne :
• C inherits from B, B inherits from A, A inherits from object
• C.__mro__ = (C, B, A, object)
• Method lookup proceeds in thest exact order

Exemple :
class A: pass
class B(A): pass
class C(B): pass
C.__mro__  # (<class 'C'>, <class 'B'>, <class 'A'>, <class 'object'>)`,
  2504: `Quand a class inherits from multiple unrelated parents, the MRO places them in the order they appear in the class definition, followed by object.

Concepts clés :
• C(A, B) means A est checked before B
• Le/La order of base classes in the class definition matters
• Unrelated parents sont linearized left-to-right
• object appears once at the end

Comment ça fonctionne :
• class C(A, B) — A est the first parent, B est the second
• C.__mro__ = (C, A, B, object)
• Attribute lookup: C → A → B → object

Exemple :
class A: pass
class B: pass
class C(A, B): pass
C.__mro__  # (<class 'C'>, <class 'A'>, <class 'B'>, <class 'object'>)`,
  2505: `Le/La order of base classes directly determines the MRO. Changing C(A, B) to C(B, A) reverses which parent est searched first.

Concepts clés :
• C(B, A) means B est checked before A
• Ce/Ceci est the opposite order from C(A, B)
• Le/La programmer controls method resolution priority by choosing base order
• Ce/Ceci est a fundamental design decision in multiple inheritance

Comment ça fonctionne :
• class C(B, A) — B est listed first, A second
• C.__mro__ = (C, B, A, object)
• Si both A and B define a method, B's version est found first

Exemple :
class A:
    x = "A"
class B:
    x = "B"
class C(B, A): pass
C.x  # "B" — B comes first in MRO`,
  2506: `Python adopted the C3 linearization algorithm in Python 2.3 (for new-style classes) and it's the only algorithm used in Python 3.

Concepts clés :
• C3 linearization guarantees a consistent and predictable MRO
• It preserves local precedence order (order of bases in class definition)
• It preserves monotonicity (if A comes before B in a parent's MRO, A also comes before B in child's MRO)
• It lève TypeError if no valid linearization exists

Comment ça fonctionne :
• C3 merges the linearizations of parent classes with the list of parents
• It picks the first head that ne ... pas appear in the tail of any other list
• Ce/Ceci process repeats until all classes sont linearized
• Si no valid head est found, TypeError est raised

Exemple :
class A: pass
class B(A): pass
class C(A): pass
class D(B, C): pass
D.__mro__  # (D, B, C, A, object) — C3 produces thest ordering`,
  2507: `Le/La diamond inheritance pattern est when two classes (B and C) inherit from the same parent (A), and a fourth class (D) inherits from both B and C. C3 linearization ensures A appears only once, after all its children.

Concepts clés :
• Diamond: A est the common ancestor of B and C, D inherits from both B and C
• A must come after both B and C in the MRO (since both depend on A)
• C3 ensures each class appears exactly once
• Le/La order of D's bases (B, C) determines B comes before C

Comment ça fonctionne :
• D(B, C): merge B's MRO (B, A, object) with C's MRO (C, A, object) and bases [B, C]
• Pick D first, alors B (head of B's MRO, not in tail of others)
• Then C (head of C's MRO), alors A, alors object
• Result: (D, B, C, A, object)

Exemple :
class A: pass
class B(A): pass
class C(A): pass
class D(B, C): pass
D.__mro__  # (D, B, C, A, object)`,
  2508: `Quand D inherits from B and C (both inheriting from A), the MRO est (D, B, C, A, object). Method lookup follows thest order.

Concepts clés :
• D ne ... pas define f — check next in MRO
• B ne ... pas define f either — check next
• C defines f returning "C" — found!
• A's f returning "A" est never reached car C's f est found first

Comment ça fonctionne :
• D().f() triggers method lookup along D's MRO: D → B → C → A → object
• D a no f, B a no f, C a f → return "C"
• Ce/Ceci demonstrates why MRO matters: even though B est listed first in D's bases, C's method wins car B ne ... pas override it

Exemple :
D.__mro__  # (D, B, C, A, object)
D().f()    # "C" — C est checked before A in the MRO`,
  2509: `C3 linearization peut fail when the inheritance hierarchy a contradictory ordering constraints. Python lève a TypeError at class creation time.

Concepts clés :
• C3 linearization a strict rules about ordering consistency
• Si parent order contradicts a base class's MRO, it fails
• Le/La error est raised when the class est defined, not when it's used
• Le/La error message says "Cannot create a consistent method resolution order"

Comment ça fonctionne :
• C3 tries to merge parent MROs respecting local precedence
• Si a class appears as a "head" in one list but in the "tail" of another, and no valid pick exists, it fails
• Ce/Ceci prevents ambiguous method resolution

Exemple :
class A: pass
class B(A): pass
class C(A, B): pass  # TypeError!
# A appears before B in C's bases, but B's MRO a A after B — contradiction`,
  2510: `Ce/Ceci est the classic example of an inconsistent MRO. Le/La conflict arises car C's base list says "A before B" but B's own MRO says "B before A".

Concepts clés :
• C(A, B) says: search A before B
• But B inherits from A, so B's MRO est (B, A, object) — B before A
• These two constraints contradict each other
• C3 linearization ne peut pas satisfy both, so TypeError est raised

Comment ça fonctionne :
• To build C's MRO, C3 must merge: A's MRO (A, object), B's MRO (B, A, object), and bases [A, B]
• A est a head in A's MRO, but A also appears in the tail of B's MRO (B, A, object)
• B est a head in B's MRO, but A must come first per C's base list
• No valid ordering exists → TypeError

Exemple :
class A: pass
class B(A): pass
class C(A, B): pass
# TypeError: Cannot create a consistent method resolution order (MRO) for bases A, B`,
  2511: `Le/La inconsistency est a direct conflict between two ordering constraints that C3 linearization must respect simultaneously.

Concepts clés :
• Constraint 1 (from C's base list): A must come before B
• Constraint 2 (from B's MRO): B must come before A (since B inherits from A)
• These two constraints sont mutually exclusive
• C3 linearization enforces both, so it fails

Comment ça fonctionne :
• C(A, B) → bases list says: A, alors B
• B(A) → B's MRO says: B, alors A
• For C's MRO, A must precede B (base list order) AND B must precede A (B's linearization)
• Both can't be true → TypeError

Correction :
class C(B, A): pass  # Ce/Ceci works! B before A est consistent with B's MRO
# C.__mro__ = (C, B, A, object)`,
  2512: `Quand both B and C override f, the MRO determines which one D uses. Since B comes before C in D's MRO, B's version wins.

Concepts clés :
• D's MRO est (D, B, C, A, object)
• D ne ... pas define f — look in B
• B defines f returning "B" — found!
• C's f and A's f sont never reached

Comment ça fonctionne :
• D(B, C) → B est listed first in bases
• D().f() → D a no f → check B → B a f → return "B"
• C's f("C") est shadowed car B appears earlier in the MRO

Exemple :
D.__mro__  # (D, B, C, A, object)
D().f()    # "B"
# Compsont with Q8 where B did NOT define f — alors C's f was used`,
  2513: `A common misconception est that super() simply calls the parent class. In reality, super() delegates to the next class in the MRO chain.

Concepts clés :
• super() renvoie a proxy that delegates to the next class in MRO
• "Next class" depends on where in the MRO the current class is
• Ce/Ceci enables cooperative multiple inheritance
• Le/La MRO est determined by the actual instance's class, not the class where super() est written

Comment ça fonctionne :
• In class B, super() points to the next class after B in the MRO of the actual object
• Si the object est of class D(B, C) with MRO (D, B, C, A, object), alors super() in B goes to C, not A
• Ce/Ceci est why super() est called "cooperative" — each class cooperates by calling super()

Exemple :
class A:
    def f(self): return "A"
class B(A):
    def f(self): return super().f() + "B"
class C(A):
    def f(self): return super().f() + "C"
class D(B, C): pass
D().f()  # "ACB" — super() in B goes to C (not A!), super() in C goes to A`,
  2514: `Ce/Ceci est the classic demonstration of cooperative super() in a diamond hierarchy. Each super() call follows the MRO, not the direct parent.

Concepts clés :
• D's MRO est (D, B, C, A, object)
• super() in B goes to C (next in D's MRO after B), NOT to A
• super() in C goes to A (next in D's MRO after C)
• Le/La call chain builds the string from the bottom up

How it works step by step:
1. D().f() → D a no f → call B.f() (next in MRO)
2. B.f(): super().f() + "B" → super() in B est C (in D's MRO)
3. C.f(): super().f() + "C" → super() in C est A (in D's MRO)
4. A.f(): return "A"
5. Back to C.f(): "A" + "C" = "AC"
6. Back to B.f(): "AC" + "B" = "ACB"

Exemple :
D().f()  # "ACB"
# Le/La key insight: super() in B goes to C, not A!`,
  2515: `Using super().__init__() in a subclass ensures the parent's initialization runs, setting up inherited attributes.

Concepts clés :
• B.__init__ calls super().__init__() which runs A.__init__
• A.__init__ sets self.a = "A" on the instance
• Then B.__init__ continues and sets self.b = "B"
• Le/La instance a both attributes: a="A" and b="B"

Comment ça fonctionne :
1. B() creates a new instance, calls B.__init__
2. super().__init__() inside B calls A.__init__(self)
3. A.__init__ sets self.a = "A"
4. Back in B.__init__, self.b = "B" est set
5. B().a accesses the attribute set by A → "A"

Exemple :
b = B()
b.a  # "A" — set by A.__init__
b.b  # "B" — set by B.__init__`,
  2516: `Without super(), diamond inheritance peut cause the shared ancestor's __init__ to run multiple times. Cooperative super() solves thest by following the MRO.

Concepts clés :
• In a diamond A ← B, A ← C, B,C ← D, A's __init__ should run once
• Si B and C both call A.__init__(self) directly, A's __init__ runs twice
• Si B and C both call super().__init__(), MRO ensures A.__init__ runs once
• Ce/Ceci est the "cooperative" pattern

Comment ça fonctionne :
• D.__init__ calls super().__init__() → goes to B (MRO: D, B, C, A, object)
• B.__init__ calls super().__init__() → goes to C (not A!)
• C.__init__ calls super().__init__() → goes to A
• A.__init__ calls super().__init__() → goes to object
• Each class's __init__ runs exactly once

Exemple :
class A:
    def __init__(self): print("A"); super().__init__()
class B(A):
    def __init__(self): print("B"); super().__init__()
class C(A):
    def __init__(self): print("C"); super().__init__()
class D(B, C):
    def __init__(self): print("D"); super().__init__()
D()  # prints D, B, C, A — each exactly once`,
  2517: `Attribute lookup follows the MRO. Le/La first class in the MRO that defines the attribute wins.

Concepts clés :
• D's MRO est (D, B, C, A, object)
• D ne ... pas define x — check B
• B defines x = 2 — found! Return 2
• C (no x) and A (x=1) sont never reached

Comment ça fonctionne :
• D.x triggers attribute lookup along the MRO
• D a no x → B a x = 2 → return 2
• Even though A also a x = 1, B est checked first

Exemple :
D.__mro__  # (D, B, C, A, object)
D.x        # 2 — from B
C.x        # 1 — C a no x, falls through to A`,
  2518: `Quand B ne ... pas define x, the MRO continues to C, which does define x=3. A's x=1 est further down the chain and never reached.

Concepts clés :
• D's MRO est (D, B, C, A, object)
• D a no x, B a no x, C a x = 3 — found!
• A's x = 1 est shadowed by C's x = 3
• Ce/Ceci demonstrates that MRO isn't just "depth first" — siblings matter

Comment ça fonctionne :
• D.x → D (no x) → B (no x) → C (x=3) → return 3
• Si C also had no x, alors A (x=1) would be found
• Le/La MRO ensures C est checked before A in the diamond

Exemple :
D.x  # 3 — from C
B.x  # 1 — B a no x, falls through to A (not C, since C isn't in B's MRO)
C.x  # 3 — C defines it directly`,
  2519: `Built-in types also ont MROs. list est a direct subclass of object with no intermediate classes.

Concepts clés :
• list inherits directly from object
• list.__mro__ = (<class 'list'>, <class 'object'>)
• Similarly, tuple.__mro__ = (<class 'tuple'>, <class 'object'>)
• Built-in types follow the same MRO rules as user-defined classes

Comment ça fonctionne :
• list a no explicit Python-level parent other than object
• Le/La MRO est simply (list, object)
• Ce/Ceci est the same structure as any simple user-defined class

Exemple :
list.__mro__   # (<class 'list'>, <class 'object'>)
tuple.__mro__  # (<class 'tuple'>, <class 'object'>)
dict.__mro__   # (<class 'dict'>, <class 'object'>)
str.__mro__    # (<class 'str'>, <class 'object'>)`,
  2520: `One of Python's lesser-known facts: bool est a subclass of int. True est 1 and False est 0 in integer context.

Concepts clés :
• bool inherits from int — thest est by design
• bool.__mro__ = (<class 'bool'>, <class 'int'>, <class 'object'>)
• True + True = 2, True * 5 = 5 — car bool IS int
• issubclass(bool, int) renvoie True

Comment ça fonctionne :
• bool was added to Python as a subclass of int for backward compatibility
• True and False sont instances of bool, which est a subclass of int
• All int operations work on booleans

Exemple :
bool.__mro__         # (<class 'bool'>, <class 'int'>, <class 'object'>)
issubclass(bool, int)  # True
isinstance(True, int)  # True
True + True            # 2`,
  2521: `super() ne ... pas return the parent class or an instance of it. It renvoie a special proxy object that knows how to forward attribute lookups to the appropriate class in the MRO.

Concepts clés :
• super() renvoie a proxy (not a class, not an instance)
• Le/La proxy delegates attribute access to the next class in the MRO
• "Next class" est relative to the class where super() est called
• Le/La proxy uses the MRO of the actual instance's type

Comment ça fonctionne :
• super() inside class B creates a proxy bound to B and self
• Quand on call super().method(), the proxy finds method in the next class after B in self's MRO
• Ce/Ceci enables cooperative method calls across the inheritance chain

Exemple :
class A:
    def greet(self): return "Hello"
class B(A):
    def greet(self):
        proxy = super()     # <super: <class 'B'>, <B object>>
        return proxy.greet() + "!"
B().greet()  # "Hello!"`,
  2522: `Python 3 introduced the zero-argument super() as syntactic sugar. Le/La explicit form super(ClassName, self) est equivalent but was required in Python 2.

Concepts clés :
• super() in Python 3 est equivalent to super(CurrentClass, self)
• Le/La first argument specifies "start searching after thest class in the MRO"
• Le/La second argument est the object (or class) to bind to
• Le/La explicit form est still useful for advanced patterns (e.g., skipping a class)

Comment ça fonctionne :
• super(B, self) means: find the next class after B in type(self).__mro__
• Si self est an instance of D with MRO (D, B, C, A, object), super(B, self) delegates to C
• super() with no arguments uses compiler magic to fill in the current class and self

Exemple :
class B(A):
    def f(self):
        return super().f()          # Python 3 shorthand
        return super(B, self).f()   # Equivalent explicit form`,
  2523: `super().f() in B delegates to A's f method, which renvoie 1. B's f alors adds 1 to get 2.

Concepts clés :
• B().f() calls B's f method
• super().f() inside B calls A's f method
• A.f() renvoie 1
• B.f() renvoie 1 + 1 = 2

Comment ça fonctionne :
1. B().f() → calls B.f(self)
2. super().f() → proxy delegates to A.f(self) → renvoie 1
3. 1 + 1 = 2
4. B.f renvoie 2

Exemple :
B().f()  # 2
A().f()  # 1`,
  2524: `Each super() call in the chain adds 1 to the result from the next class in the MRO.

Concepts clés :
• C's MRO est (C, B, A, object)
• C.f() calls super().f() → B.f()
• B.f() calls super().f() → A.f()
• Each level adds 1

Comment ça fonctionne :
1. C().f() → C.f(self)
2. super().f() in C → B.f(self) → super().f() in B → A.f(self) → 1
3. B.f renvoie 1 + 1 = 2
4. C.f renvoie 2 + 1 = 3

Exemple :
A().f()  # 1
B().f()  # 2
C().f()  # 3
# Each level adds 1 through the super() chain`,
  2525: `super() est not restricted to __init__. It peut be used in any instance method, class method, or property to delegate to the next class in the MRO.

Concepts clés :
• super() works in __init__, regular methods, class methods, and properties
• It always follows the MRO regardless of which method it's used in
• Common uses include extending parent behavior in any method
• Le/La only restriction est that it ne ... pas work in static methods (no instance/class context)

Comment ça fonctionne :
• super() needs either an implicit or explicit class and instance/class reference
• In instance methods: super() uses the class where it's defined and self
• In class methods: super() uses the class where it's defined and cls
• In static methods: no self or cls available, so super() ne ... pas work

Exemple :
class A:
    def greet(self): return "Hello"
    @classmethod
    def create(cls): return cls()

class B(A):
    def greet(self): return super().greet() + " World"
    @classmethod
    def create(cls): return super().create()

B().greet()   # "Hello World"
B.create()    # <B instance>`,
  2526: `super() works with @classmethod. In Python 3, super() inside a classmethod automatically uses the correct class context.

Concepts clés :
• @classmethod receives cls instead of self
• super() inside a classmethod still works correctly
• super().f() delegates to A's classmethod f
• Le/La result est "A" + "B" = "AB"

Comment ça fonctionne :
1. B.f() → calls B's classmethod f with cls=B
2. super().f() → proxy delegates to A.f(cls=B) → renvoie "A"
3. "A" + "B" = "AB"
4. B.f() renvoie "AB"

Exemple :
A.f()  # "A"
B.f()  # "AB"
# super() in classmethods follows the same MRO rules`,
  2527: `super(A, A()) creates a proxy that looks up methods starting from the class after A in A's MRO.

Concepts clés :
• A's MRO est (A, object)
• super(A, instance) means: start looking after A in the MRO
• Le/La next (and only remaining) class after A est object
• So super(A, A()) proxies for object

Comment ça fonctionne :
• super(A, A()) — first arg est "start after A", second arg est the instance
• A.__mro__ = (A, object)
• After A in the MRO comes object
• Any method call on thest proxy will look in object

Exemple :
class A: pass
s = super(A, A())
s.__init__    # <method-wrapper '__init__'> — from object
type(s)       # <class 'super'>`,
  2528: `super().__init__() permet the subclass to pass specific arguments to the parent's __init__ while adding its own initialization.

Concepts clés :
• B.__init__ takes both x and y
• super().__init__(x) forwards only x to A.__init__
• A sets self.x = x (which est 1)
• B alors sets self.y = y (which est 2)
• Le/La instance a both attributes

Comment ça fonctionne :
1. B(1, 2) calls B.__init__(self, 1, 2)
2. super().__init__(1) calls A.__init__(self, 1)
3. A sets self.x = 1
4. Back in B, self.y = 2
5. b.x = 1, b.y = 2

Exemple :
b = B(1, 2)
b.x  # 1 — set by A.__init__
b.y  # 2 — set by B.__init__`,
  2529: `Le/La order of print statements depends on where super().__init__() est called relative to the print in B.__init__.

Concepts clés :
• B.__init__ runs: print("B") first, alors super().__init__()
• super().__init__() calls A.__init__ which prints "A"
• Result: "B" est printed first, alors "A"
• Si super().__init__() were called before print("B"), the order would reverse

Comment ça fonctionne :
1. B() → B.__init__(self)
2. print("B") → outputs "B"
3. super().__init__() → A.__init__(self)
4. print("A") → outputs "A"
5. Output: B, alors A

Exemple :
B()
# Output:
# B
# A`,
  2530: `Ce/Ceci shows super() used in a regular method (not __init__) to extend the parent's behavior.

Concepts clés :
• B.greet() calls super().greet() to get A's result
• A.greet() renvoie "Hello"
• B.greet() concatenates " World" to get "Hello World"
• Ce/Ceci est the "extend parent behavior" pattern

Comment ça fonctionne :
1. B().greet() → B.greet(self)
2. super().greet() → A.greet(self) → "Hello"
3. "Hello" + " World" = "Hello World"
4. Return "Hello World"

Exemple :
A().greet()  # "Hello"
B().greet()  # "Hello World"
# super() lets you build on the parent's implementation`,
  2531: `super() relies on knowing which class it's called from and having a reference to the instance or class. Static methods provide neither.

Concepts clés :
• super() with no arguments uses compiler magic (__class__ and self/cls)
• @staticmethod a no self or cls parameter
• Without an instance or class, super() ne peut pas determine the MRO position
• Calling super() in a staticmethod lève RuntimeError or TypeError

Comment ça fonctionne :
• In instance methods: super() uses __class__ (implicit) and self
• In class methods: super() uses __class__ (implicit) and cls
• In static methods: no self, no cls → super() fails
• You could use super(ClassName, instance) explicitly, but that defeats the purpose

Exemple :
class A:
    @staticmethod
    def f():
        super().f()  # RuntimeError: super(): no current class
# Le/La fix: don't use super() in static methods, or use @classmethod instead`,
  2532: `Using super() est the recommended practice car it enables cooperative multiple inheritance and avoids hardcoding parent class names.

Concepts clés :
• super().__init__() follows the MRO — correct for diamond inheritance
• ParentClass.__init__(self) hardcodes the parent — brittle, peut cause double calls in diamonds
• super() adapts when the class hierarchy changes
• ParentClass.__init__(self) must be manually updated if inheritance changes

Comment ça fonctionne :
• In a diamond (D → B → C → A), super().__init__() in B calls C.__init__ (MRO order)
• But B calling A.__init__(self) directly skips C entirely
• Ce/Ceci peut cause A.__init__ to run twice (once from B, once from C)
• super() prevents thest by ensuring each __init__ runs exactly once

Exemple :
# BAD — hardcoded parent:
class B(A):
    def __init__(self):
        A.__init__(self)  # Breaks in diamond inheritance

# GOOD — cooperative super():
class B(A):
    def __init__(self):
        super().__init__()  # Follows MRO correctly`,
  2533: `Le/La cooperative multi-init pattern solves the problem of passing different arguments to different classes in a multiple inheritance chain.

Concepts clés :
• Each class accepts **kwargs in __init__
• Each class extracts (pops) only the arguments it needs
• Remaining kwargs sont forwarded to super().__init__(**kw)
• object.__init__() receives empty kwargs at the end

Comment ça fonctionne :
• class A: def __init__(self, **kw): self.a = kw.pop("a", 0); super().__init__(**kw)
• class B: def __init__(self, **kw): self.b = kw.pop("b", 0); super().__init__(**kw)
• class C(A, B): def __init__(self, **kw): super().__init__(**kw)
• C(a=1, b=2) → A pops a=1, passes b=2 to B → B pops b=2, passes {} to object

Exemple :
class A:
    def __init__(self, **kw):
        self.a = kw.pop("a", 0)
        super().__init__(**kw)
class B:
    def __init__(self, **kw):
        self.b = kw.pop("b", 0)
        super().__init__(**kw)
class C(A, B):
    pass
c = C(a=10, b=20)
c.a  # 10
c.b  # 20`,
  2534: `Le/La key rule of cooperative inheritance est that each class in the MRO chain must cooperate by forwarding unhandled arguments.

Concepts clés :
• Each class pops only the keyword arguments it needs
• All remaining kwargs sont forwarded via super().__init__(**kw)
• Ce/Ceci ensures every class in the MRO gets its required arguments
• object.__init__() at the end should receive no extra kwargs

Comment ça fonctionne :
• Class pops its args: self.x = kw.pop("x", default)
• Forwards the rest: super().__init__(**kw)
• Next class in MRO does the same
• Eventually object.__init__() est reached with empty kwargs

Why thest matters:
• Without thest pattern, multiple inheritance __init__ conflicts sont hard to resolve
• Each class only needs to know its own arguments
• Adding a new class to the hierarchy ne ... pas require modifying existing classes
• Le/La MRO determines which class processes kwargs in which order`,
  2535: `Ce/Ceci question traces the full super() call chain in a diamond, showing how each class contributes to the final list.

Concepts clés :
• D's MRO: (D, B, C, A, object)
• super() in D goes to B, super() in B goes to C, super() in C goes to A
• Le/La list est built from the bottom (A) up through each super() return

How it works step by step:
1. D().f() → D.f: return super().f() + ["D"] → calls B.f
2. B.f(): return super().f() + ["B"] → calls C.f (not A!)
3. C.f(): return super().f() + ["C"] → calls A.f
4. A.f(): return []
5. Back to C: [] + ["C"] = ["C"]
6. Back to B: ["C"] + ["B"] = ["C", "B"]
7. Back to D: ["C", "B"] + ["D"] = ["C", "B", "D"]

Exemple :
D().f()  # ['C', 'B', 'D']
# Le/La result reflects the reverse traversal of the MRO (A→C→B→D)`,
  2536: `Multiple inheritance permet a class to combine capabilities from multiple parents. Quand methods don't conflict, both sont simply inherited.

Concepts clés :
• Duck inherits from both Flyable and Swimmable
• Flyable fournit fly(), Swimmable fournit swim()
• No method name conflicts — both sont available on Duck instances
• Ce/Ceci est a common pattern for combining orthogonal behaviors

Comment ça fonctionne :
• class Duck(Flyable, Swimmable) inherits all methods from both parents
• d.fly() → found in Flyable → renvoie "flying"
• d.swim() → found in Swimmable → renvoie "swimming"
• Duck's MRO: (Duck, Flyable, Swimmable, object)

Exemple :
d = Duck()
d.fly()   # "flying"
d.swim()  # "swimming"
isinstance(d, Flyable)    # True
isinstance(d, Swimmable)  # True`,
  2537: `Mixins sont a design pattern in Python for adding functionality to classes without creating deep inheritance hierarchies.

Concepts clés :
• A mixin fournit specific functionality (logging, serialization, etc.)
• Mixins sont not meant to be instantiated on their own
• They sont "mixed in" via multiple inheritance
• Convention: name them with a Mixin suffix (e.g., LogMixin, JSONMixin)

Comment ça fonctionne :
• Define a mixin class with useful methods
• Other classes inherit from the mixin alongside their main parent
• Le/La mixin's methods become available on the child class
• Multiple mixins peut be combined

Exemple :
class LogMixin:
    def log(self, msg):
        print(f"[LOG] {msg}")

class SerializeMixin:
    def to_dict(self):
        return self.__dict__

class User(LogMixin, SerializeMixin):
    def __init__(self, name):
        self.name = name

u = User("Alice")
u.log("created")    # [LOG] created
u.to_dict()         # {"name": "Alice"}`,
  2538: `Ce/Ceci demonstrates the simplest mixin pattern: a class inherits a utility method from a mixin.

Concepts clés :
• LogMixin fournit a log() method
• App inherits from LogMixin, gaining the log() method
• App().log("hello") calls LogMixin.log(self, "hello")
• Le/La f-string formats the message as "LOG: hello"

Comment ça fonctionne :
• class App(LogMixin) inherits all of LogMixin's methods
• App().log("hello") → LogMixin.log(self, "hello")
• f"LOG: {msg}" → f"LOG: hello" → "LOG: hello"

Exemple :
app = App()
app.log("hello")    # "LOG: hello"
app.log("started")  # "LOG: started"`,
  2539: `Le/La JSONMixin fournit serialization capabilities to any class that uses it. json.dumps produces a JSON-formatted string with double quotes.

Concepts clés :
• self.__dict__ renvoie the instance's attribute dictionary: {"name": "Alice"}
• json.dumps() converts a Python dict to a JSON string
• JSON always uses double quotes for strings
• Le/La mixin peut be added to any class to provide to_json()

Comment ça fonctionne :
• User("Alice") creates an instance with self.name = "Alice"
• self.__dict__ = {"name": "Alice"}
• json.dumps({"name": "Alice"}) = '{"name": "Alice"}'
• Note: JSON uses double quotes, Python repr uses single quotes

Exemple :
u = User("Alice")
u.__dict__     # {'name': 'Alice'}
u.to_json()    # '{"name": "Alice"}'  (JSON string with double quotes)`,
  2540: `By convention and for correctness, mixins sont listed before the main parent class in the inheritance list.

Concepts clés :
• class MyClass(MixinA, MixinB, MainParent) — mixins first
• Left-to-right order determines MRO priority
• Mixin methods override main parent methods if names conflict
• Ce/Ceci ensures mixin behavior takes precedence

Comment ça fonctionne :
• MRO processes bases left to right
• class C(Mixin, Base) → MRO: C, Mixin, Base, object
• Si both Mixin and Base define method(), Mixin's version est used
• Placing Mixin first gives it priority

Exemple :
class LogMixin:
    def save(self):
        print("logging save")
        return super().save()

class Model:
    def save(self):
        print("saving to DB")

class User(LogMixin, Model):
    pass

User().save()
# Output: "logging save" alors "saving to DB"
# LogMixin.save runs first, alors calls super().save() → Model.save`,
  2541: `Ce/Ceci mixin fournit timestamp functionality to any class. Each call to stamp() renvoie the current datetime.

Concepts clés :
• stamp() est an instance method added by the mixin
• It imports datetime and renvoie datetime.now()
• Le/La timestamp est generated at call time, not at object creation
• Any class inheriting TimestampMixin gets thest method

Comment ça fonctionne :
• class MyClass(TimestampMixin): pass
• MyClass().stamp() calls TimestampMixin.stamp(self)
• datetime.now() renvoie the current date and time
• Each call renvoie a new timestamp

Exemple :
class Event(TimestampMixin):
    def __init__(self, name):
        self.name = name

e = Event("meeting")
e.stamp()  # datetime.datetime(2024, 1, 15, 10, 30, 45, ...)
# Returns current datetime each time stamp() est called`,
  2542: `Unlike some languages, Python ne ... pas raise an error when multiple parents define the same method. Le/La MRO simply determines which one est used.

Concepts clés :
• Multiple parents peut ont methods with the same name
• Python ne ... pas raise any error — it uses MRO to resolve the conflict
• Le/La first class in the MRO that defines the method wins
• You peut still access shadowed methods explicitly (e.g., ParentB.method(self))

Comment ça fonctionne :
• class C(A, B) where both A and B define f()
• C's MRO: (C, A, B, object)
• C().f() → A.f() est found first (A comes before B in MRO)
• B.f() est shadowed but still accessible via B.f(instance)

Exemple :
class A:
    def greet(self): return "Hello from A"
class B:
    def greet(self): return "Hello from B"
class C(A, B): pass

C().greet()        # "Hello from A" — A est first in MRO
B.greet(C())       # "Hello from B" — explicit call to B's version`,
  2543: `Quand two unrelated parents both define the same attribute, the order of bases determines which one est used.

Concepts clés :
• C inherits from A and B, both of which define x
• C's MRO: (C, A, B, object)
• C.x → C a no x → A a x = 1 → found! Return 1
• B's x = 2 est shadowed

Comment ça fonctionne :
• Attribute lookup follows the MRO: C → A → B → object
• A est checked before B car A est listed first in C(A, B)
• A.x = 1 est found, so the search stops
• B.x = 2 est never reached

Exemple :
C.x  # 1 — from A (first in bases)
# Si you change to class C(B, A): pass
# Then C.x would be 2 — from B (now first)`,
  2544: `Swapping the base order from C(A, B) to C(B, A) changes which parent's attribute est found first.

Concepts clés :
• C's bases sont now (B, A) instead of (A, B)
• C's MRO: (C, B, A, object)
• C.x → C a no x → B a x = 2 → found! Return 2
• A's x = 1 est shadowed thest time

Comment ça fonctionne :
• Le/La order of bases directly controls the MRO
• C(B, A) means B est searched before A
• B.x = 2 est the first match

Exemple :
class C(B, A): pass
C.x  # 2 — from B
# vs
class C(A, B): pass
C.x  # 1 — from A
# Le/La order of bases est a deliberate design choice`,
  2545: `These two relationships sont fundamental OOP concepts that determine how classes relate to each other.

Concepts clés :
• Inheritance ("is-a"): Car est a Vehicle — Car inherits Vehicle's interface and behavior
• Composition ("has-a"): Car a an Engine — Car contient an Engine instance
• Inheritance creates tight coupling between classes
• Composition creates loose coupling and est often preferred

Comment ça fonctionne :
• Inheritance: class Car(Vehicle) — Car IS a Vehicle
• Composition: class Car: def __init__(self): self.engine = Engine() — Car HAS an Engine
• With inheritance, changing Vehicle affects Car
• With composition, Engine peut be changed independently

Exemple :
# Inheritance (is-a):
class Vehicle:
    def start(self): return "starting"
class Car(Vehicle): pass  # Car IS a Vehicle

# Composition (has-a):
class Engine:
    def start(self): return "vroom"
class Car:
    def __init__(self):
        self.engine = Engine()  # Car HAS an Engine`,
  2546: `"Favor composition over inheritance" est a well-known design principle. Composition fournit more flexibility at the cost of slightly more code.

Concepts clés :
• Composition permet swapping components at runtime
• Inheritance creates a rigid, tightly coupled hierarchy
• Composition makes testing easier (inject mock components)
• Inheritance est appropriate when there's a true "is-a" relationship

Quand to use composition:
• Quand on want to reuse behavior without committing to a type hierarchy
• Quand the relationship est "has-a" or "uses-a"
• Quand on need to change behavior at runtime
• Quand on want to combine behaviors from multiple sources without MRO complexity

Quand to use inheritance:
• Quand the relationship est genuinely "is-a" (Dog IS an Animal)
• Quand on need polymorphism (treat derived types as base type)
• Quand on want to shsont interface and implementation

Exemple :
# Composition — flexible:
class Logger:
    def log(self, msg): print(msg)
class App:
    def __init__(self, logger=None):
        self.logger = logger or Logger()  # Can swap loggers!`,
  2547: `Ce/Ceci est a textbook example of composition. Car ne ... pas inherit from Engine; it contient an Engine and delegates work to it.

Concepts clés :
• Car "has-a" Engine (composition)
• Car.start() delegates to self.engine.start()
• Car ne ... pas inherit Engine's interface — it wraps it
• Le/La Engine peut be replaced with a different implementation

Comment ça fonctionne :
• Car.__init__ creates an Engine instance: self.engine = Engine()
• Car.start() calls self.engine.start() and renvoie the result
• Car est not a subclass of Engine — it's a wrapper/container
• Ce/Ceci fournit flexibility: you could pass a different engine type

Exemple :
class ElectricEngine:
    def start(self): return "whirr"

class Car:
    def __init__(self, engine):
        self.engine = engine
    def start(self):
        return self.engine.start()

Car(Engine()).start()          # "vroom"
Car(ElectricEngine()).start()  # "whirr"  — swap at runtime!`,
  2548: `Ce/Ceci est the simplest form of inheritance. Car est a Vehicle and inherits all of Vehicle's methods.

Concepts clés :
• Car(Vehicle) means Car IS a Vehicle
• Car inherits start() from Vehicle without redefining it
• Car instances sont also Vehicle instances
• isinstance(Car(), Vehicle) renvoie True

Comment ça fonctionne :
• class Car(Vehicle): pass — Car inherits everything from Vehicle
• Car().start() → Vehicle.start(self) → "starting"
• No methods sont overridden or added
• Car est a specialization of Vehicle

Exemple :
c = Car()
c.start()                # "starting" — inherited from Vehicle
isinstance(c, Vehicle)   # True — Car IS a Vehicle
isinstance(c, Car)       # True
Car.__mro__              # (Car, Vehicle, object)`,
  2549: `Quand inheriting from three unrelated parents, the MRO includes the class itself, all three parents in order, and object.

Concepts clés :
• D(A, B, C) inherits from three parents
• D's MRO: (D, A, B, C, object) — 5 entries
• Parents appear in the order listed in the class definition
• object appears once at the end

Comment ça fonctionne :
• D est first in its own MRO
• A, B, C follow in the order they appear in D(A, B, C)
• object est the ultimate base, appearing last
• len(D.__mro__) = 5

Exemple :
D.__mro__
# (<class 'D'>, <class 'A'>, <class 'B'>, <class 'C'>, <class 'object'>)
len(D.__mro__)  # 5`,
  2550: `Python permet subclassing built-in types like list, dict, str, int, etc. Le/La subclass inherits all the built-in behavior.

Concepts clés :
• class MyList(list) creates a subclass of the built-in list type
• MyList instances sont also list instances
• All list methods (append, pop, sort, etc.) sont inherited
• You peut override or add methods to customize behavior

Comment ça fonctionne :
• MyList(list) inherits from list
• MyList([1, 2, 3]) creates a MyList initialized with [1, 2, 3]
• isinstance(ml, list) renvoie True — MyList IS a list
• MyList.__mro__ = (MyList, list, object)

Exemple :
class MyList(list):
    def first(self):
        return self[0] if self else None

ml = MyList([1, 2, 3])
ml.first()            # 1
ml.append(4)          # Works — inherited from list
isinstance(ml, list)  # True
len(ml)               # 4`,